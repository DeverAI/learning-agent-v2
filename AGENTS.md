# AGENTS.md —— 本项目工作区专属规则与故障库索引

> 本文件是**索引**，不是内容副本（FreqErr 明确记过「文档多源漂移」：同一份说明抄到多处，
> 改一处就产生互相冲突的"当前状态"）。要看内容请点进对应文件。

## 项目一句话

学习 Agent（学习搭子）：面向一名上海初三学生的本地优先学习系统。
三端 = `backend/`（FastAPI + Jinja2 原生前端 + 网页/PWA）、`desktop/`（PySide6 桌面端，网课旁听/讲题/打断）、
`android/`（WebView 壳 + 原生 TTS/相机/权限桥）。目标是撑过中考知识补漏与自招学习。

## 先读什么（按需，别全量灌）

| 想知道 | 读 |
|---|---|
| 本轮/最新状态、待办 | `todo.md`（头部有最新一轮链接） |
| 某一轮的完整检修过程与结论 | `updates/` 目录（**R23 = 20260912_检修03_九大重点_R23.md**） |
| 功能块边界（从代码盘点，不信文档） | `功能块清单.md` |
| 当前设计边界与数据归属 | `Design.md` |
| 已知错误模式（**动手前先查这里**） | `FreqErr.md`（按 `## [模式名]` 检索） |
| 需求定稿 | `updates/20260911_新功能需求定稿.md`、`Agent双端架构设计.md` |

## 本项目的硬规则（踩过坑总结出来的）

1. **改前备份**：改任何文件前，先把**改动前**内容复制到 `backups/audit_rNN_<日期>/`（保持原相对路径）。
   回滚 = 把备份复制回原位。
2. **文件操作走工具，不走命令行**（用户红线）：不要用 pwsh/bash 增删改复制项目文件。
3. **改完必须跑全量回归**：`python -m pytest backend`（R23 后基线 **258 passed**）。
   桌面端另有离线 harness：用 Python312 跑 `desktop/test_r*.py`（需要 PySide6）。
4. **前端断言要"三处同改"**：Pydantic 模型 → 路由 GET/PUT 响应 → 前端 init/save；
   新增设置字段还要进 `config.load_settings` 与 `ai_service._reload()`（FreqErr 有专条）。
5. **新增工具必须同时登记三处**：`_TOOL_SPECS`、`_HANDLERS`、处理器函数（有测试钉住；
   漏了就成"不可达功能"，且分类器 prompt 由表生成、不会报错）。
6. **新增审计标记类型**必须同时进 `audit_service.FLAG_TYPES` 与 `_PRESERVED_FLAG_TYPES`，
   否则夜间自动重写会把它清掉（现象："标记出现过一会儿又没了"）。
7. **不伪造成功**：降级/兜底值必须与正常值可区分（`degraded: true`、`status: error`）；
   "没有数据"不得冒充"数据正常"。这条是产品红线。
8. **测试要按意图断言**：R17 有过"测试把错误假设固化成契约"的事故（512 tokens 就够），
   写断言前先确认真实阈值/真实行为。
9. **.ps1 含中文必须带 UTF-8 BOM**（与 .py/.js 相反）；远端调 nssm 必须 `cmd /c`；
   **永不用 `nssm restart/stop`**（会把服务打停），用强杀 + 自动拉起。

## 部署（小心操作）

- 服务器：`ssh mindog`（8.138.12.209）→ `C:\all_projects\learningAgent\backend\`，nssm 服务 `LearningAgent`（8000）
- 下载页：http://8.138.12.209:8000/download ｜ 网页版：http://8.138.12.209:8000/
- 部署脚本与铁律见 `todo.md` 末尾「部署备忘」。**部署前先确认**（不可逆动作先停手问用户）。
- `gradlew` 在中文路径下失效（R14）：安卓出包需在 ASCII 路径构建。

## 已知的"看不出来的坑"（详见 FreqErr.md）

- **零调用点 = 功能不可达**：实现 + 单测 + 生产没人调 → 测试全绿也发现不了（R23 撞到两条）。
- **硬编码默认口令兜底**：改密码即全站 401（R23 已修成"401 补密重试"）。
- **TextToSpeech 4000 字符上限**：超限静默不说（R23 已按句分片）。
- **WebView 没有 Service Worker、没有 Web Speech API**：离线靠 IndexedDB 层；语音识别在安卓端不可用。
- **GBK 控制台**：日志/print 含生僻字符会静默丢行（探针里的 print 还会把自己抛的异常记到被测代码头上）。
- **pytest 先收集后执行**：测试里重定向 `config.*` 必须遵守"首导入方"惯例，别无条件覆盖。
