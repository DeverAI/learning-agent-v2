# Agent 双端架构设计（前台交互 / 后台推理）

> 日期：2026-09-11 ｜ 作者：Mavis ｜ 状态：架构设计稿（待确认后实施）
> 需求来源：用户要求 Agent 有「前后两端」——**一个跟人交互，一个推理找内容**；**跟人交互的那个可以决定要不要打断推理的那个**；**两者本质上都是同一个 agent**
> 确认项：交付物=先设计后改代码 ｜ 打断=**前台 Agent 自主判断** ｜ 推理端=**通用底座 + 提前备课**
>
> 关联：[自招素材系统技术方案.md](自招素材系统技术方案.md)（提前备课的下游消费者）

## 0. 结论速览

| # | 决策 | 理由 |
|---|------|------|
| 1 | **一个内核 + 两种执行模式**，不是两个 agent | 用户原话「本质上也都是一个agent」。共用工具注册表、上下文组装、记忆 |
| 2 | **通用底座的落点是「工具注册表」** | 现在是 839 行 if/elif 硬分支。抽成声明式工具表后，加功能只需注册 |
| 3 | **打断判定并入现有意图分类调用** | 本来就要调一次轻量模型，多一个 JSON 字段即可，**近乎零额外成本** |
| 4 | **取消用协作式，不用 `Task.cancel()` 打底** | 项目已有「原子写 + 状态机」纪律；硬取消会在写盘中途抛 CancelledError，留半条数据 |
| 5 | **先拆锁，否则一切白做** | `_get_session_lock(sid)` 让整会话串行，前后台根本无法并行 |
| 6 | **后台任务状态必须落库** | 现在 step/tool 是进程内存 dict，重启即丢，撑不住分钟级后台任务 |
| 7 | **复用 `ProcessingTask` 范式，不另造轮子** | 项目已有「持久化 + 重启恢复 + 状态机 + 单键锁」的成熟基建 |
| 8 | **后台并发上限 = 5** | 用户确认（2026-09-11）。不是 2。允许后台多线程并行 |
| 9 | **15 个工具全部注册进工具表** | 用户确认。注意工具之间的交互（§4.2） |
| 10 | **提前备课 = 用户手动触发**，且**可不只讲课** | 用户确认：不是空闲自动；要能**筛选题目 / 试卷**等素材，备完即可「上课」消费（§8） |
| 11 | **前台发现用户问题 → 自主选择「继续钻研」或「放弃」** | 用户确认（2026-09-11）。这是打断之外的第二种前台决策（§5.5） |
| 12 | **全程注意多端同步** | 网页 / Android WebView / 桌面端共用模板与后端，改动必须三端一起核（§10.1） |

## 1. 需求还原

| 用户原话 | 落到设计 |
|----------|----------|
| 一个跟人交互的 | **前台 Foreground**：面向用户、低延迟、可随时接收新输入 |
| 一个推理找内容的 | **后台 Background**：执行长任务、检索、生成、预生成 |
| 跟人交互的可以决定要不要打断推理的 | **打断判定**：用户新消息到达时，前台自主决策 cancel / keep / observe（§5） |
| 他们本质上也都是一个 agent | **共享内核**：同一个工具注册表 + 同一份 session/profile/记忆 |
| 通用底座 | **工具注册表**：前台/后台只是工具的 `foreground` 标志不同（§4） |
| 增加提前备课功能 | 后台端的一个调度消费者（§8） |

## 2. 现状诊断：为什么这不是小改

### 2.1 现有 Agent 全貌

```
POST /api/sessions/{sid}/chat
  └─ session_chat()                                  [sessions.py:1097]
       └─ _session_chat_impl(sid, req)               [sessions.py:258-1096]  ← 839 行单函数
            ├─ Phase 1: 轻量模型意图分类（MiMo V2.5 免费 / DeepSeek flash 兜底）
            │    返回 {type, data, steps}
            ├─ Phase 2: 15 种意图的 if/elif 硬分支
            │    chat / solve / search / solve_q / paper / auto_paper /
            │    add_question / edit_question / style / profile / need /
            │    list_notes / get_note / create_note / modify_note /
            │    save_image / error_info
            └─ 返回 {reply, steps, action, tool_calls}
```

配套机制：

| 机制 | 实现 | 位置 |
|------|------|------|
| 会话串行锁 | `_get_session_lock(sid)`，每会话一把 `asyncio.Lock` | `sessions.py:43` |
| 步骤记录 | `_agent_step_stores`：**进程内存 dict**，按 sid 键，LRU 上限淘汰 | `ai_service.py:100-116` |
| 工具调用记录 | `_tool_call_stores`：同上，内存 dict | `ai_service.py:119-141` |
| 步骤回调 | `_make_step_callback(sid)` → `agent_steps_add(parent, child, status)` | `ai_service.py:159` |
| 步骤构建 | `_build_steps` 优先用 AI 返回的层级步骤，否则按 `STEP_LABELS` 造默认 3 步 | `sessions.py:95` |
| 前端进度 | `agent.html` 的 `startStepsPoll()` 轮询 `GET /api/sessions/{sid}/steps` | `agent.html:380` |
| 多轮 | `agent_multi_turn()` **定义了但无人调用——死代码** | `ai_service.py:1223` |

### 2.2 四个硬障碍

**障碍 1：串行锁掐死了并发**

`_get_session_lock(sid)` 是**整会话粒度**。只要前台在等 AI，后台什么都做不了；反过来后台一跑，用户发消息就得排队。**双端架构的第一刀必须切在这里。**

**障碍 2：状态在内存里，后台任务扛不住**

`_agent_step_stores` / `_tool_call_stores` 是进程内存 dict，重启即丢。前台对话几百毫秒无所谓，**但后台任务可能跑几分钟，中间还跨重启**。必须落库。

**障碍 3：没有任务生命周期概念**

现在只有「请求-响应」一个模型。没有任务 ID、没有状态机、没有取消入口、没有「已产出部分保留」。后台推理端需要一整套。

**障碍 4：工具是硬编码分支**

839 行 if/elif。用户要的「通用底座」要求**声明式工具表**——否则每加一个后台能力就要再动这个巨型函数，越改越危险。

**好消息**：项目里已经有 `ProcessingTask` 的成熟范式（Techniques.md §3）——先安全写文件/DB 再建任务、应用持引用防 GC、重启按状态恢复、单键一把锁、`staged→pending→processing→generating_solution→成功/error` 状态机。**后台推理端应该推广这套范式，而不是另造一套。**

## 3. 目标架构

### 3.1 一个内核 + 两种执行模式

```
┌──────────────────────────────────────────────────────────────┐
│                     Agent 内核（共享）                        │
│  · 工具注册表 ToolRegistry                                    │
│  · 上下文组装（session / profile / hippocampus / 记忆）        │
│  · 意图分类器                                                 │
│  · 步骤与工具调用记录（带 fg/bg 维度）                         │
└───────────────┬──────────────────────────┬───────────────────┘
                │                          │
    ┌───────────▼────────────┐  ┌──────────▼──────────────────┐
    │ 前台 Foreground         │  │ 后台 Background             │
    │                        │  │                             │
    │ · 面向用户，低延迟       │  │ · 执行长任务                │
    │ · 快工具同步执行         │  │ · 检索 / 生成 / 预生成       │
    │ · 慢工具丢后台并立即回话  │  │ · 提前备课                  │
    │ · 持有打断决策权 ────────┼─▶│ · 可被取消（协作式）         │
    │                        │  │ · 状态落库、可续跑           │
    └────────────────────────┘  └─────────────────────────────┘
                ▲                          │
                └──── 共享同一 session ─────┘
```

**「本质上是同一个 agent」的落地方式**：前台和后台**不是两个 bot**，而是同一内核的两种执行模式。它们共用：
- 同一套工具注册表（区别只在工具的 `foreground` 标志）
- 同一份 session / profile / 海马体记忆
- 同一套步骤与工具调用记录（只是多了一个 `origin: fg|bg` 字段）

区别只有两点：**谁面对用户**、**能不能被取消**。

### 3.2 一次请求的完整时序

```
用户发消息
   │
   ├─▶ 前台：意图分类（一次轻量模型调用）
   │     返回 {type, data, steps, interrupt:{decision, target_task_id, reason}}
   │
   ├─▶ 【打断判定】decision == cancel → 协作式取消后台任务
   │                decision == keep   → 后台照跑
   │                decision == observe→ 把新消息作为增量喂给后台
   │
   ├─▶ 命中前台工具 → 同步执行 → 返回 reply
   │
   └─▶ 命中后台工具 → 建任务、落库、立即返回
                     reply = "我去准备了，你先忙，好了叫你"
                     → 前端出现一张可取消的后台任务卡片
```

## 4. 通用底座的落点：工具注册表

把 15 种意图从 if/elif 抽成声明式表：

```python
# backend/services/agent_core.py
@dataclass
class Tool:
    name: str                 # 工具名
    label: str                # 中文显示名
    handler: Callable         # 执行体
    foreground: bool          # True=前台同步；False=可丢后台
    cancellable: bool         # 是否允许被打断
    needs_net: bool = False   # 是否联网（决定超时与重试）
    timeout: int = 120        # 秒
    description: str = ""     # 供意图分类器使用

TOOLS: dict[str, Tool] = {
    "chat":          Tool("chat",         "闲聊",     h_chat,        True,  False),
    "solve":         Tool("solve",        "解题",     h_solve,       True,  False),
    "search":        Tool("search",       "搜索题库", h_search,     True,  False),
    "prepare_lesson":Tool("prepare_lesson","提前备课",h_prepare,     False, True,
                          needs_net=True, timeout=900),
    # ...
}
```

**收益**：
1. 加功能 = 注册一条，不再改巨型函数
2. 意图分类器的 prompt 可由 `TOOLS` **自动生成**（现在那段 prompt 是手写硬编码的中文长串，极易与分支失去同步——这是个已存在的隐患）
3. `foreground` / `cancellable` / `timeout` 成为**数据**，前后台分流是查表而不是硬判断

**范围确认（2026-09-11）**：**15 个工具全部注册**，不保留任何硬编码分支。

### 4.1 全量工具表与归属

| 工具 | 中文名 | 归属 | 可取消 | 备注 |
|------|--------|------|--------|------|
| `chat` | 闲聊 | 前台 | — | 最快路径 |
| `solve` | 解题 | 前台 | — | 需推理模型 |
| `search` | 搜索题库 | 前台 | — | 查库 |
| `solve_q` | 找题并解 | 前台 | — | search + solve 复合 |
| `paper` | 组卷 | **前台（长）** | 是 | 已登记「同步挂起 20-30s」问题（见 done.md §5），应优先转后台 |
| `auto_paper` | 直接出卷 | **后台** | 是 | 与 `paper` 同类 |
| `add_question` | 新增题目 | 前台 | — | 写库 |
| `edit_question` | 修改题目 | 前台 | — | 写库 |
| `style` | 改偏好 | 前台 | — | 写 profile |
| `profile` | 存个人信息 | 前台 | — | 写 profile |
| `need` | 功能请求 | 前台 | — | 兜底分支 |
| `list_notes` | 笔记列表 | 前台 | — | 查库 |
| `get_note` | 查看笔记 | 前台 | — | 查库 |
| `create_note` | 创建笔记 | 前台 | — | 写库 + 图谱同步（有历史坑 M5） |
| `modify_note` | 修改笔记 | 前台 | — | 写库 + 图谱同步（有历史坑 M5） |
| `save_image` | 存图待处理 | 前台 | — | **已登记死路**（前端零引用，见 done.md §5） |
| `error_info` | 查看错误 | 前台 | — | 查库 |
| `prepare_lesson` | **提前备课** | **后台** | 是 | 新增（§8） |

> 表内 17 行为 17 个动作，其中 `paper`/`auto_paper`/`prepare_lesson` 三个具备后台属性；其余以前台为主。**确切归属需按 §4.2 的交互分析最终定稿。**

### 4.2 工具交互（用户特别提示）

工具不是彼此独立的，注册时必须显式声明交互关系，否则前后台分流会踩坑：

| 交互类型 | 涉及工具 | 风险 | 处理 |
|----------|----------|------|------|
| **复合调用** | `solve_q` = `search` + `solve`；`auto_paper` ≈ `paper` + 检索 | 拆成注册表后复合关系丢失，变成两次独立调用 | 显式声明 `composes: ["search", "solve"]` |
| **写同一份数据** | `add_question` / `edit_question` 写题库；`create_note` / `modify_note` 写笔记 + 图谱 | 后台任务与前台同时写 → 竞态 | 复用既有 `asyncio.Lock` 纪律（Techniques.md §12） |
| **共享侧写** | `create_note` / `modify_note` → `refresh_note_snippet` 图谱同步 | 历史坑 M5：绕过图谱同步 | 后台路径必须走同一条正规链 |
| **参数来源重叠** | `search` / `paper` / `auto_paper` / `prepare_lesson` 都要 subject/grade | 意图分类混淆（用户说"选几道题"可能命中任意一个） | 在 `description` 里写清边界，供分类器判别 |
| **前后台同触** | `prepare_lesson`（后台）产出的题目/试卷，可能被 `paper`（前台）再次消费 | 后台备课未完成时前台就用了半成品 | 产出物带 `ready` 状态，未 ready 不可消费 |

## 4.3 关于「注意他们的交互」的一条推论

由于 `paper` 已经登记「同步挂起 20-30s」，而 `auto_paper` 与它同源——**建议把 `paper` 也划入后台**，前台立即返回「正在组卷，好了叫你」。这样既解决 20-30s 挂起，又和 `prepare_lesson` 共用同一套任务机制。**但这会改变组卷的现有交互体验，需用户确认**（已列入 §12 待决）。

**风险**：15 个处理器从 839 行函数里拆出来，是机械但量大的重构，且每个处理器都依赖 `_load`/`_save`/`profile`/`style` 等上下文。建议**先抽 3 个代表性工具**（`chat` 最快、`solve` 中等、`prepare_lesson` 全新）验证模式，再批量迁移，避免一次性动 839 行。

## 5. 打断协议（前台自主判断）

### 5.1 零成本实现：并入意图分类

前台每次都要调一次轻量模型做意图分类。**把打断判定并入这次调用**，只多一个 JSON 字段：

```json
{
  "type": "chat",
  "data": {},
  "steps": [...],
  "interrupt": {
    "decision": "cancel",
    "target_task_id": "task_ab12",
    "reason": "用户在否定当前备课方向，改问别的章节"
  }
}
```

**判定输入**（塞进同一次调用的 system prompt）：
- 用户新消息
- 正在跑的后台任务摘要：`task_id` + 目标 + 进度 + 已产出摘要

**为什么这样设计**：不新增模型调用、不新增延迟、不新增成本。意图分类本来就在做「理解用户这句话要干什么」——**「要不要打断」是同一件事的另一面**。

### 5.2 三态决策

| 决策 | 触发情形 | 动作 |
|------|----------|------|
| `cancel` | 用户否定方向、换话题、明确喊停 | 协作式取消任务，保留已产出 |
| `keep` | 用户只是闲聊、补充无关内容 | 后台照跑，互不影响 |
| `observe` | 用户补充了约束（"要上海的真题""别太长"） | 后台继续跑，但把新信息作为**增量输入**注入 |

**`observe` 是这套设计里最有价值的一档**：用户不必推倒重来，随时能追加约束。这是「前台不打断、但也不忽略」的中间态。

### 5.3 取消机制：协作式优先

```
后台任务执行循环：
  for step in steps:
      if task.cancel_requested:          ← 每个关键检查点主动检查
          task.status = "cancelled"
          save_partial_output()          ← 已产出的部分保留
          return
      await do_step(step)                ← I/O 点
```

**为什么不用 `asyncio.Task.cancel()` 打底**：`CancelledError` 可能在写文件或写 DB 中途抛出，留下半条数据。项目已有明确纪律（Techniques.md §12：JSON 原子写、并发 CRUD 用 `asyncio.Lock`、状态更新按顺序）。**协作式取消能在检查点上安全退出**，与既有纪律一致。

`Task.cancel()` 只作为**兜底**：任务卡死在非检查点的网络 I/O 时，超时后强杀。

### 5.4 任务状态机

```
pending → running → (done | cancelled | error)
              ↑         │
              └─ 续跑 ──┘（cancelled 且已有部分产出时，可从这个状态续跑）
```

**关键：`cancelled` 不是终点。** 被取消的任务保留已产出内容与断点，用户可以要求「接着刚才的继续」——这与 [自招素材系统](自招素材系统技术方案.md) 的断点续播是同一套思路。

## 6. 锁拆分（必须第一步做）

现在的锁是**整会话粒度**，必须拆：

| 锁 | 粒度 | 覆盖范围 |
|----|------|----------|
| `fg_lock(sid)` | 会话 | 前台对话串行（防止同一会话两条消息并发） |
| `bg_task_lock(task_id)` | 任务 | 后台任务自身的状态更新串行 |
| ~~整会话锁~~ | — | **移除** |

**原则**：前台不等后台。用户发消息时，后台任务正在跑，前台必须能立即响应。

沿用项目既有做法：锁表用弱引用释放已完成键（Techniques.md §3），避免锁对象无限累积。

## 7. 状态与展示：双线

### 7.1 状态落库

新增 `AgentTask` 表（推广 `ProcessingTask` 范式）：
**实施状态（2026-09-11 R11）：表已落地**，在 `backend/models/models.py`，表名取 `agent_tasks`
（与既有 `processing_tasks` / `upload_sessions` / `saved_configs` 的复数命名一致，
本文档原写的单数 `agent_task` 已按实际实现更正）。`progress` 按项目惯例用 `Float` 而非 `Integer`
（与 `ProcessingTask` 对齐），比原设计的 0-100 整数更细。其余字段与下表一致，
另加 `cancel_requested` 布尔位承载协作式取消。
建表**不需要**动 `database.py` 的迁移清单——`init_db()` 的 `create_all` 会建新表，
迁移清单只用于给**已存在**的表加列。

```python
class AgentTask(Base):
    __tablename__ = "agent_tasks"
    id          = Column(String, primary_key=True, default=gen_id)
    sid         = Column(String, index=True)      # 所属会话
    tool        = Column(String)                  # 工具名，见 §4
    title       = Column(String)                  # 显示名，如「提前备课：宋代经济」
    params      = Column(JSON, default=dict)      # 入参
    status      = Column(String, default="pending")  # pending/running/done/cancelled/error
    progress    = Column(Integer, default=0)      # 0-100
    steps       = Column(JSON, default=list)      # 该任务的步骤
    output      = Column(JSON, default=dict)      # 产出引用（material_id / 文件路径）
    partial     = Column(JSON, default=dict)      # 取消时的中间态，供续跑
    cancel_requested = Column(Boolean, default=False)
    error       = Column(Text, default="")
    created_at  = Column(DateTime, default=_utcnow)
    updated_at  = Column(DateTime, default=_utcnow, onupdate=_utcnow)
```

同时 `_agent_step_stores` / `_tool_call_stores` 增加 `origin` 维度（`fg` / `bg`），并考虑改为落库（至少后台部分必须落库）。

### 7.2 前端双线展示

`agent.html` 现在是单线：`#agentSteps` + `startStepsPoll()`。需要扩展为：

```
┌─ 前台步骤（现有 agentSteps）────────────────┐
│ 意图识别 → 解析消息            done         │
│ 提前备课 → 准备素材            running      │
└────────────────────────────────────────────┘

┌─ 后台任务卡片（新增）───────────────────────┐
│ 🔄 提前备课：宋代经济            [取消]     │
│    进度 45% · 已检索 6 条资料                │
│    · 检索资料          done                  │
│    · 生成讲解稿        running               │
└────────────────────────────────────────────┘
```

- 后台卡片带**取消按钮**（用户显式取消通道，与前台自主判断并存）
- 轮询接口扩展为同时返回两条线（§9）

## 8. 提前备课

**用户手动触发，不是空闲自动**（2026-09-11 确认）。原设计中「挂进 `night_patrol` 空闲调度」一节作废。

### 8.1 定位：备课 ≠ 只写讲解稿

**备课的产物是一份「可上课的课包」**——含**筛选过的题目 / 试卷** + 讲解稿 + 板书 + 音频。

| | 纯讲课（❌ 不是这个） | **课包（✅ 是这个）** |
|---|---|---|
| 原料 | 模型凭空生成 | **从题库 / 试卷里筛出来的真实题目** |
| 过程 | 直接写稿 | **先素材选取，再讲解编排** |
| 产出 | 一段讲解稿 | 题集 + 讲稿 + 板书 + 音频 |

### 8.2 输入：可筛选题目 / 试卷

| 筛选维度 | 来源 | 说明 |
|----------|------|------|
| 学科 / 年级 | 用户指定 | |
| 知识点 / 主题 | 用户指定 | |
| **题目范围** | 题库 `Question` | 按学科/年级/标签/难度取题 |
| **试卷范围** | 已有试卷 `Paper` | 直接复用现成卷，不重新拼 |
| 题量 / 时长 | 用户指定 | 决定任务规模与超时 |
| 来源偏好 | 本地题库 / 联网检索 | 决定 `needs_net` |

**这是与「纯讲课」的本质区别**：备课的第一阶段是**素材选取**，第二阶段才是**讲解编排**。

### 8.3 产出：课包 Lesson Pack

| 成分 | 落地 |
|------|------|
| 选中的题目 / 试卷 | 引用 `Question` / `Paper` id |
| 讲解稿 | `materials.body` |
| 板书 | 复用专注模式的 `board.ops` 结构 |
| 预生成音频（分段） | `material_audio`（见 [自招素材系统技术方案.md](自招素材系统技术方案.md) §11.11） |

### 8.4 「备完就可以上课」

课包状态置 `ready` 后，前台可直接进入「上课」消费：
- 走专注模式既有链路（分段讲解 + 检查点 + 黑板）
- 或走自招素材系统的音频播放链路

**硬约束**：课包未 `ready` 时**不可消费**（对应 §4.2 最后一行）——防止前台拿到半成品。

### 8.5 触发与任务特性

- **触发**：用户显式「帮我备课」→ 前台建后台任务 → **立即回话，不干等**
- **特性**：耗时长（筛题 + 检索 + 生成 + TTS），**必须可暂停、可取消、可续跑**
- **为什么它是理想的首个用例**：天然后台、天然需打断、天然有断点、下游落库目标已设计好

## 8.6 前台的第二种决策：继续钻研 or 放弃

除了打断（§5.1–5.4），前台还有第二种自主决策：**发现用户存在问题（知识薄弱点 / 答错 / 反复在同一处卡住）时，选择「继续钻研之前的问题」还是「放弃」**。

| 决策 | 情形 | 动作 |
|------|------|------|
| **钻研** | 判定是关键卡点、值得深挖 | 建后台任务深挖（查资料 / 生成针对性练习 / 追根溯源），**前台继续正常对话不阻塞** |
| **放弃** | 判定投入产出比低、或偏离当前目标 | 记录为「已识别但暂不处理」，之后不再反复触发 |

**与打断的关系**：打断是「对**正在跑**的任务做决策」，这里是「对**要不要新建**任务做决策」。两者**共用同一次判定调用**（并入意图分类，见 §5.1），不增加成本。

**必须落库**：被识别的问题与决策结果要记录，否则同一问题会被反复识别、反复打扰用户。建议新增 `AgentFinding` 表（或复用海马体记忆系统）。

**风险**：这是最容易被模型滥用的功能——判太松会疯狂建后台任务，判太紧则毫无价值。建议 v1 **只识别不自动建任务**，先只做「记录 + 提示用户要不要钻研」，等语料验证判定质量后再放开自动化。

## 9. 接口设计

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/api/sessions/{sid}/chat` | **保持兼容**，返回体新增 `interrupt` 与 `background_tasks[]` |
| GET | `/api/sessions/{sid}/steps` | **扩展**：返回 `{foreground: [...], background: [{task_id, title, status, progress, steps}]}` |
| GET | `/api/agent/tasks?sid=` | 列出某会话的后台任务 |
| GET | `/api/agent/tasks/{task_id}` | 单任务详情 |
| POST | `/api/agent/tasks/{task_id}/cancel` | 用户显式取消 |
| POST | `/api/agent/tasks/{task_id}/resume` | 从 `cancelled` 续跑 |
| POST | `/api/agent/tasks` | 显式创建后台任务（如"帮我备课"） |

**兼容性原则**：`/chat` 的既有字段（`reply` / `steps` / `action` / `tool_calls`）**一个都不删**，只增不减，避免前端与 Android 壳同时爆炸。网页端与 Android WebView 是同一套模板，改一处即两处生效，但**必须一起回归**。

## 10. 改动清单（文件级）

> **实施状态（2026-09-11 R11）**：下表逐行标注。R11 已完成「工具注册表」这一前置项，
> 其余（后台推理端 / 拆锁 / 打断判定 / 任务端点 / 双线展示）尚未开始。
> 详见 `updates/20260911_Agent工具注册表与内容接入_R11.md`。

| 文件 | 改动 | 量级 | R11 状态 |
|------|------|------|---------|
| **新增** `backend/services/agent_core.py` | Agent 内核：工具注册表、上下文组装、意图分类（含打断判定）、日志 | 大 | **已完成**（注册表 + Ctx + prompt/标签生成 + 超时执行；打断判定未做） |
| **新增** `backend/services/agent_tools.py` | 23 个工具的处理器与工具表（原 19 + 新增 4 个内容工具） | 大 | **已完成**（本文档原未列此文件；实现时按"注册表在 core、处理器在 tools"拆开，避免单文件过长） |
| **新增** `backend/services/background_agent.py` | 后台推理端：任务创建、执行循环、协作式取消、状态持久化、续跑 | 大 | 未开始 |
| `backend/routers/sessions.py` | **拆 839 行函数**；拆锁；接入打断判定；`/steps` 扩展 | **大（主战场）** | **部分**：函数已拆完（1113 → 360 行，分支全搬进 agent_tools）；**拆锁未做**；打断判定未做 |
| `backend/models/models.py` | 新增 `AgentTask` 表 | 小 | **已完成** |
| `backend/models/database.py` | 迁移清单追加 `agent_task` | 小 | **不需要**：新表由 `create_all` 建；迁移清单只管给已有表加列 |
| `backend/services/ai_service.py` | `agent_steps_*` / `agent_tool_calls_*` 增加 `origin` 维度；`agent_multi_turn` 死代码清理或接入 | 中 | **仅加了 `glm_web_search`**（可自证的联网检索）；`origin` 维度与 `agent_multi_turn` 未动 |
| `backend/routers/system.py` | 空闲调度（提前备课）挂进 `night_patrol` 范式 | 中 | 未开始 |
| **新增** `backend/routers/agent_tasks.py` | 任务管理端点组 | 小 | 未开始 |
| `backend/templates/agent.html` | 双线展示 + 后台任务卡片 + 取消按钮 | 中 | **部分**：已加 4 类结构化动作卡片（related_content / web_search / knowledge_lookup / recall_memory）；后台任务卡片与取消按钮未做 |
| `Design.md` §5 / `Techniques.md` | 同步架构文档（项目约定） | 小 | 未做 |

**注意**：`_session_chat_impl` 的 15 个分支里，多个分支含 `.append(...)` 直接改文件、以及 `agent_tool_calls_add` 调用，拆分时要保证**行为等价**——建议拆完用现有测试（`backend/test_*.py` 有 18 个测试文件）做回归基线。

**R11 实测**：拆分后 `pytest` **160 passed**，与拆分前一致；分类器路由实跑 **9/9 命中**。
拆分过程中发现并修掉三处真实缺陷（`edit_paper`/`delete_paper` 死分支、`edit_paper` 缺确认闸、
`delete_paper` 的确认闸建在 LLM 输出上），另自查拦下两处**我自己引入的**风险
（默认超时等于硬取消写盘、部署脚本输出被吞）。详见 R11 记录文档。

### 10.1 多端同步（用户特别提示）

本项目是**一套模板、多端消费**：

| 端 | 形态 | 与后端的关系 |
|----|------|--------------|
| 网页端 | FastAPI + Jinja 模板 | 直接 |
| Android | WebView 壳，加载同一套模板 | **同一套模板**，改一处即生效 |
| 桌面端 | `desktop/`（独立副本，OISystem 文化课分支） | **独立代码**，需单独同步 |

**改动纪律**：

1. **网页 + Android 自动同步**（同一模板），但**必须一起回归**——不存在"只改网页"。
2. **桌面端需单独同步**。`desktop/` 是整仓拷贝的独立副本，后端/前端改动**不会自动传导**；涉及 Agent 架构的改动若桌面端也要跟进，需单列同步清单。
3. **APK 与桌面端 zip 是发布产物**，后端改动后需重新出包（上一轮做法：`/download` 页 + `static/downloads` 直出，见 done.md §4）。
4. **缓存与版本**：PWA 有离线缓存（`ENABLE_OFFLINE_CACHE`），模板改动需注意 SW 缓存失效（见 Design.md §13.2）。


## 11. 分期与验收

| 阶段 | 内容 | 验收 |
|------|------|------|
| **P1** | **拆锁** + `AgentTask` 表 + 任务状态机（不接任何工具） | 前台对话期间后台能跑一个空转任务，互不阻塞 |
| **P2** | 工具注册表抽 3 个代表工具（`chat` / `solve` / `prepare_lesson`） | 意图分类 prompt 由 `TOOLS` 自动生成；三个工具行为与现状一致 |
| **P3** | 后台执行循环 + 协作式取消 + 状态落库 + 续跑 | 取消后无半条脏数据；`cancelled` 任务能续跑 |
| **P4** | 打断判定并入意图分类（三态） | 三类语料各 10 条，判定正确率可接受 |
| **P5** | 前端双线展示 + 取消按钮 | 前台步骤与后台任务卡片同屏，取消按钮生效 |
| **P6** | 提前备课接入 + 空闲调度 | 空闲时自动备课，产出落到 `materials` |
| **P7** | 其余 12 个工具批量迁移 | 全部走注册表，839 行函数拆分完成 |

**P1 是硬前置**：不拆锁后面全是空谈。**P2 是关键验证点**：注册表模式若在 3 个工具上跑不通，后面 12 个也不用迁。

## 12. 风险与待决

### 风险

| # | 风险 | 应对 |
|---|------|------|
| 1 | 839 行函数拆分引入行为回归 | P2 只动 3 个工具验证模式；用现有 18 个测试文件做基线 |
| 2 | 打断判定误杀（该 keep 的 cancel 了） | 三态设计 + `cancelled` 可续跑 → **误杀代价可控**，这是设计成可续跑的主因 |
| 3 | 打断判定漏杀（该 cancel 的没 cancel） | 保留用户显式取消通道（后台卡片按钮）兜底 |
| 4 | 前后台并发写同一 session JSON | 现在 `_save` 无锁保护跨进程；需明确「前台只写对话，后台只写任务」，或 session 文件按字段分片 |
| 5 | 状态从内存迁到 DB 的兼容 | 保留内存快路径（前台步骤仍可内存），仅后台任务强制落库 |
| 6 | 意图分类 prompt 与工具表失同步 | 由 `TOOLS` 自动生成 prompt，从根上消除（这本身是现状的隐患） |

### 待决（需要用户输入）

| # | 问题 | 影响 |
|---|------|------|
| 1 | ~~后台任务并发上限~~ | **已确认 = 5**（2026-09-11）。允许多线程并行 |
| 2 | 后台任务是否有**资源配额**（token / 联网次数） | 并发 5 后更需配额，否则成本不可控；项目多模型可路由到便宜模型 |
| 3 | 取消后**是否自动保留产出**，还是询问用户 | 影响交互与存储 |
| 4 | ~~提前备课触发时机~~ | **已确认：用户手动触发**（2026-09-11） |
| 5 | 后台任务完成后**如何「叫你」** | 网页端可轮询；硬件端（见自招素材系统 §11）需另设计 |
| 6 | `paper` 是否也划入后台（§4.3） | 能解决已登记的「组卷同步挂起 20-30s」，但改变现有交互 |
| 7 | 「继续钻研 / 放弃」（§8.6）v1 是否只识别不自动建任务 | 建议 v1 先只记录+提示，防模型滥用 |
| 8 | `AgentFinding` 是否复用海马体记忆系统，还是新建表 | 影响表结构 |
