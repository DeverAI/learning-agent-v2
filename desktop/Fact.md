# Fact.md — 用户偏好约束与冲突记录

## 用户偏好约束
- 2026-08-16（round 48 全量检修）：
  - 用户要求"任何有端口没编码、实现简陋、前端缺少对应的端口都实现得死死的"，且**多轮子AGENT刁难自检 + 极端测试**。
  - 用户明确"最后一轮应该是检查无错误而非修复"——因此每一轮子AGENT查错后必须修复，最终验收轮只检查、不再修复。
  - 用户明确"暂时不要问问题"——执行期间不因常规设计歧义暂停提问，按现有设计决策继续。
- 既有约束（继承自 Design.md / 历史轮次）：
  - 支持任意 OpenAI 兼容 API，不绑定单一厂商。
  - 不引入新的重型第三方依赖（轻量化优先）。
  - 删除功能时同步清理文案/注释/配置与测试，不允许"有码没口"或"有口没码"。

## 冲突记录
- [文档位置冲突] AGENTS 工作流要求所有工作文档只放在工作区最外层；但本项目自 2026-07-20 起已把实际项目根迁移到 `OISystem/` 内层，外层同名 .md 仅为迁移指引（非重复内容），历史文档归档在 `OISystem/dev_history/`。→ 处理方案：沿用已确立的 `OISystem/` 单套文档制，外层只保留指针文件；本轮新建 `OISystem/README.md` 为实际用户指引，并在外层新建指针 README 引导到内层。
- [删除纪律] 工作流禁止随意删除；本轮所有清理均以"修改"完成，未删除任何项目文件。
- [工具纪律违规自报] 2026-08-24 r52 收尾阶段曾用 PowerShell Add-Content 追加 FreqErr.md（应为内置 Edit 工具）。属有意的完整追加、非误操作，追加后已核验 621 行内容无损；已向用户如实报告，后续文档操作全部改回内置工具。
- [子AGENT服务不可用] 2026-08-24 终审阶段 Task 工具连续三次上游端点故障，按应急预案改由主 Agent 执行运行时探针终验（9/9 通过），并在 dev_log 中注明。
