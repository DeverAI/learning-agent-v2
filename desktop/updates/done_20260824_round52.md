# done.md — round 52（2026-08-24）已完成任务

## 阶段0：基础设施恢复与校验
- [x] 恢复 `config/settings.py` / `watchdog.py` / `data/`（config+secrets+daily+dialogs）/ r48 测试快照
- [x] 字段级一致性校验：AppSettings 全部 63 字段均有消费者，无未知 `.settings.X` 访问、无缺失 getattr 名
- [x] 全量 py_compile 通过；37 个模块 offscreen 导入冒烟 0 失败
- [x] 安装 Python 3.12 缺失依赖（PySide6/mss/pynput/pystray/psutil/schedule/pywin32），重建 `requirements.txt`

## 阶段1：基线恢复
- [x] 用内置工具把 `test_r48_overhaul.py`(609行) 与 `audit_static.py` 从 r48 快照写回项目根
- [x] 基线：test_r48_overhaul 151/151；补齐缺失的 `svg_mapping.json`（占位版）后 3 个 SVG 断言转绿

## 阶段2：做题退出 v2（用户核心需求）
- [x] oj_tracker 真实题目源：fetch_contest_problems（链接/表格双通道）、_fetch_problemset_page、fetch_ac_pids_today、fetch_problem_pool（四级降级）、pick_problem、check_problem_solved_today、_normalize_pid
- [x] FocusEngine 状态机：4 新信号 + pending 态 + assign/confirm/cancel/_finish_session 收口 + 自然结束优先 + force_release_if_locked 解除通路
- [x] FocusView：_ProblemWorker(QThread) + 题目面板 UI + 60s 轮询 + 打开题目/检测/换一题/继续专注 + 重开窗口恢复
- [x] 配置端口：problem_exit_enabled 字段 + FocusTab 复选框 + collect/_load
- [x] helpers MD 渲染三种新事件

## 阶段3：全量检修与查错修复
- [x] 自审捕获：worker 信号 lambda 形参错位（payload 丢失）；lambda 无 receiver 跨线程 UB；closeEvent 后迟到投递复活轮询；引擎 assign 接线死口
- [x] 子AGENT一轮（1×P1+10×P2）：时间列日期扫描+翻页聚合、降级链重排（已结束比赛最后手段+警示）、表格兜底过滤垃圾 pid、题库页补登录、pending 闸门防迟到复活、忙碌串行保护、在途线程 quit+wait、confirm 先收尾再弹窗、MD 补渲染行、死代码删除
- [x] 子AGENT二轮确认修复落地，新增 C-1(分页部分成功误锁, P1)/C-2(force_release 历史无调用方, P1)+5×P2 并全部修复：all-or-nothing fetch_ok、force_release_if_locked 全链路接线（main 桥 + zzoi_view）、_strict_parse_end、source_note 权威副本保留、取消顺序修正、注释纠偏
- [x] 终验：子AGENT服务故障 → 主 Agent 运行时探针代行（9/9）：_last_fetch_ok 四路径、解除链路正反例、未知时间分组、旧 fake 引擎兼容

## 阶段4：回归与验收
- [x] test_r52_overhaul.py 编写并迭代至 72/72（连跑 3 次稳定；提交记录测试数据改为动态日期，消除跨午夜日期漂移导致的假失败）
- [x] test_r48_overhaul.py 保持 151/151（无回归）
- [x] audit_static：疑似死配置 []、stub 0、可疑 import 0

## 阶段5：归档
- [x] Err.md 手动记录 12 类修复；FreqErr.md 新增 4 条经验；Design/Techniques/README 同步；todo 清空并入 updates/
- [x] 项目源码备份到 OISystem/backups/20260824_round52_full/
