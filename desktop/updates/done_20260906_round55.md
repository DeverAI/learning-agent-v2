# done.md — round 55 完成清单（2026-09-06/07：课堂音频双通道 + VAD + MiMo ASR + 打断决策引擎）

> 设计依据：Design.md「课堂感知音频通道与打断决策引擎（round 55）」+「round 55 实现后的设计补强」。
> 实现细节与审查修复清单：Techniques.md round 55 节 §1-§8。
> 归档：本文件已存 `updates/done_20260906_round55.md`；DevLog 见 `dev_log/20260906_round55.md`。

| # | 任务 | 状态 | 结果 |
|---|------|:----:|------|
| 1 | `core/audio_capture.py` 纯 ctypes WASAPI 采集（零新依赖） | ✅ | 实机验证：48kHz 立体声 float32 混音格式，loopback 抓取 RMS 0.34/峰值 0.56，mic 通道正常；COM 资源全链释放（ReleaseBuffer try/finally 兜底、S_FALSE 配对 CoUninitialize） |
| 2 | `core/vad.py` 能量法 VAD（零依赖） | ✅ | 修 3 个真缺陷（onset 属性名/秒帧换算/段尾静音未裁）；合成信号验证 2 段切分、0.1s 短音丢弃、5s 连续语音强制切 3 段不丢内容 |
| 3 | `ai_client.transcribe_audio()` MiMo ASR | ✅ | 实测逐字转写正确（4/4 关键词）；M1 修复后语义分级：空 content=静音段返回空串，结构异常抛错计入 failed 触发暂停保护 |
| 4 | 设置项扩展（config/settings.py） | ✅ | 15+ 字段（classroom_*/interrupt_*/xiaomi_asr_model）+ float 归一 + interrupt_level 白名单 + `_INT_RANGES` 收紧（M2: abs_floor_db≤-10；M3: max_segment_sec≤30）；脏值实测全部归位 |
| 5 | `core/classroom_stream.py` 课堂文字流编排 | ✅ | 双路→VAD→单转写线程→Qt 信号→jsonl 落盘；C2/M4 修复（每会话新 Event + drain 排空遗留 + stop 时序重排）；H1 通道看门狗；dropped_stale 记账；隐私红线内置 |
| 6 | `core/interrupt_engine.py` 打断决策引擎 | ✅ | 五级闸门纯函数 + 冷却 + 静音 + request_teaching 旁路；H2 修复（interrupt 字符串显式归一）；M5 修复（上下文新鲜度闸门 STALE_CONTEXT_SEC=300，fail-open）；决策走 resolve_dialog_target（v4-pro） |
| 7 | `test_r55_classroom.py` 离线回归 | ✅ | T1-T16 共 **221 PASS / 0 FAIL**（含 T16 审查修复回归锁 18 项）；compileall 通过 |
| 8 | `demo_classroom.py` 实机端到端验证 | ✅ | TTS 放音 12.0s → loopback → VAD 单段 → MiMo 转写三句全对、关键词 4/4、failed=0；期间发现并修复 WASAPI 空闲停产包致末段滞留的真缺陷（空闲静音注入 250ms 门槛） |
| 9 | 子 Agent 故障检测（两轮） | ✅ | 第 1 轮：C1/C2/H1/H2/M1-M5/L1-L7 共 16 项；第 2 轮（修复复核）：14/14 修复确认正确 + 3 Low 当场再修（channel_error 首参统一 speaker、drain 记账、L4 注释口径）；终判 VERDICT: PASS |
| 10 | 归档 + 检查点备份 | ✅ | todo 清空、done/updates/dev_log 归档、Err.md（OISystem 运行表）清空保留表头、backups/classroom_20260906/ 检查点 |

## 关键数据
- 回归测试：221 PASS / 0 FAIL（Python 3.12 + PySide6 offscreen）
- 端到端：12.0s 讲课音频 → 单段完整转写，关键词 4/4
- 修复统计：本轮自检 6 项（pack=1、单块超限裁剪、空闲静音注入、WAVEFORMATEX、测试 bug ×3）+ 审查 19 项（C1/C2 破坏性 + H1/H2 红线级 + M1-M5 + L1/L3/L4/L5/L7 + Low×3）
- 零新第三方依赖（ctypes COM 直调 WASAPI）
