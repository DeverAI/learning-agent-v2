# done.md — round 56 完成清单（2026-09-07：课堂 AI 教师联动层 + 设置接线）

> 设计依据：Design.md「课堂 AI 教师联动层与设置接线（round 56）」。
> 实现细节与审查修复清单：Techniques.md round 56 节。
> 归档：本文件已存 `updates/done_20260907_round56.md`；DevLog 见 `dev_log/20260907_round56.md`。

| # | 任务 | 状态 | 结果 |
|---|------|:----:|------|
| 1 | `core/classroom_stream.py` suppress_teacher | ✅ | 回环防护窗 + 过期 VAD reset（同线程串行）+ stats 记账；审查后持锁防丢改 |
| 2 | `core/classroom_coach.py` 联动编排器 | ✅ | 订阅 interrupt_triggered → 右上角局部卡片 → v4-pro 讲解词**流式上板** → TTS 朗读；fail-safe 回退 teach_point；重入守卫；决策轮询**工作线程化**（审查 C1） |
| 3 | `core/ai_client.py` chat_stream | ✅ | SSE 流式（on_delta 累积文本契约）；审查后补 resp.close + timeout 120s |
| 4 | `main.py` 启动接线 | ✅ | 双开关 → 三件套 + coach + toast；失败记 Err 降级不阻断；aboutToQuit 清理 |
| 5 | `ui/settings_view.py` ClassroomTab | ✅ | 7 控件（总开关/loopback/mic/分级/冷却/置信度/朗读）+ 生效提示 + collect 往返 |
| 6 | `test_r56_coach.py` 离线回归 | ✅ | **57 PASS / 0 FAIL**（T1-T7：suppress/折行/卡片/主链/收尾/流式 fail-safe/设置往返） |
| 7 | `demo_coach.py` 实机验证 | ✅ | **5/5 PASS**；用户三反馈全修复复验：局部卡片不遮挡、字号钳制、流式上板；线程违规告警消除 |
| 8 | 子 Agent 审查 + 修复 | ✅ | C1（evaluate 阻塞主线程 5-60s）→ 决策工作线程化；H2（stream 连接泄漏）→ resp.close；M3（suppress 竞态）→ 持锁+同线程 reset；M4（防护窗不含合成时延）→ playing 补窗；L5/L6 顺手修 |
| 9 | 归档 + 检查点备份 | ✅ | todo 清空、done/updates/dev_log/FreqErr、Err.md 清空、backups/coach_20260907/ |

## 用户实测反馈 → 修复对照
| 反馈 | 修复 |
|------|------|
| ① 全屏白板关不掉 | 局部卡片（右上角 44%×32%）+ Ctrl+Alt+H 全局热键 + 30s 自动清除 + 底部提示 |
| ② 字体 AI 不会掌握 | 字号双向钳制（标题 16-26px、正文 13-19px），卡片布局固定 |
| ③ "生成中"很奇怪，可开流式 | ai_client.chat_stream（SSE）+ partial 节流（0.4s/12 字）逐句上板 |
| （附）串台疑问 | 非 bug：demo_coach.py 实机验证预期行为；本轮起实机前先告知 |

## 修复统计
- 实机复现修复 3 处（信号跨线程 UB ×2：coach 闭包连接 + tts_player r53 遗留闭包连接）
- 子 Agent 审查修复 6 项（1 Critical + 1 High + 2 Medium + 2 Low）
- 零新第三方依赖（pynput/requests 均已有）
