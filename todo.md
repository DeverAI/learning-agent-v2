# todo.md —— 2026-09-12（R4→R27：… R26 智能上传/小组件 + **R27 深检真缺陷 + 试卷 PDF 导入**）

> 回归基线：`python -m pytest backend` → **320 passed**（R27 后）
> **最新一轮：`updates/20260912_深检_智能上传真入库与PDF导入_R27.md`**
> R26：`updates/20260912_智能上传与小组件_R26.md`；R25：`updates/20260912_检修05_改密课稿难度打断_R25.md`

## ⚠️ 最重要的一条：系统内容为空的实测结论（2026-09-12）

| 项 | 服务器（实际在用） |
|---|---|
| 题库 / 笔记 / 试卷 / 批改 / 课稿 | **1 / 0 / 0 / 0 / 0** |
| 知识图谱（R18 前） | **文件不存在** → R18 已灌入 206 知识点 |
| 学习记忆 `hippocampus/profile.json` | **文件不存在**（= 专注/讲题从未在服务器产生过学习记录） |
| 会话文件 | **0** |

**推论：所有基于题库/笔记/掌握度的功能都在空转；"知识补漏"缺骨架（R18 已补）；还没有一条真实学习数据。**
下一步的真正瓶颈是**把内容灌进来**，不是继续加功能。

## 用户已确认的需求（2026-09-12）

- **材料形态**：① 手机拍的错题/试卷照片（OCR 管线已有）② 电子版真题/试卷 PDF ③ 课本/教辅 PDF（要按知识点切）
- **自招目标**：**课内 + 自招并行，自招要难一档的题**（→ 需要难度分层与难题内容源）


## 下载入口（用户点名要的）

- 下载页 **http://8.138.12.209:8000/download**
- APK v1.4（versionCode 5，含 TTS 桥）：**http://8.138.12.209:8000/static/downloads/学习搭子-Android.apk**
- 桌面端源码包（2026-09-11）：**http://8.138.12.209:8000/static/downloads/学习搭子-桌面端-20260911.zip**
- 网页版 / PWA：**http://8.138.12.209:8000/**
- 页面上的体积与日期**由服务器读取磁盘真实文件**渲染，不再是手写值（手写值已漂移过一次）


## 已完成（R4→R10）

- [x] **R4** 回归基线修复（pytest 全量崩溃 → 137 → **160 passed**）+ 三路代码审计 16 项修复
- [x] **R4** 纠正我自己用正则得出的错误结论（改 AST），避免错修 4 个正常文件
- [x] **R5 / R5b** 全项目文档线索穷尽挖掘（**49 条未做/待做**）+ 4 项补实现 + 文档漂移修正 3 处
- [x] **R6** 三端讲题 Agent 审计（约 45 项假实现）+ 讲题链路 7 处修复（取号崩溃 / 非原子写 / 序号覆盖 / 422 / 无限递归 / 导航死锁 / 两个按钮死锁）
- [x] **R7** 停止向长期记忆写入伪造值（`degraded` 标记 + 真实时长 + `None` 不当 0 + 语音特征不编造）
- [x] **R7** 部署流程修正：`nssm restart/stop` 会打停服务 → 改**强杀 + nssm 自动拉起**（实测通过）
- [x] **R8** 后端 8 + 前端 4 + 桌面端 2 + Design.md 3 处
- [x] **R9** 后端 F4/F7 + 桌面端 F2/F3/F4/F5/F7F8
- [x] **R10** 黑板子系统审计 + 8 问判定；修 **三层断点**（桌面端视觉选位从未运行）、**`/api/notes` 漏鉴权头**（课堂笔记从未同步）、**我自己引入的 2 个 bug**（截断日志 `NameError` / `hide_overlay` 被 `_repaint` 反效）
- [x] **R10** 交接摘要与用户纠正已落文档

## 已完成（R11：Agent 工具注册表 + 相关内容接入）

- [x] **R11** 工具注册表落地：新增 `services/agent_core.py`（内核）+ `services/agent_tools.py`（23 个工具的处理器与表）；`routers/sessions.py` **1113 → 360 行**
- [x] **R11** 修 **`edit_paper` / `delete_paper` 死分支**——有实现但不在分类器 prompt 里，**从来没执行过一次**；prompt 与标签改为**由表生成**，从根上消除三处失同步
- [x] **R11** 补 `edit_paper` 缺失的确认闸（原注释声称有、实际没有）
- [x] **R11** 修 **`delete_paper` 确认闸建在 LLM 输出上**——分类器自己会塞 `confirm: true`，闸门自己会开；改为统一走**用户原话确认语**
- [x] **R11** 新增 `AgentTask` 表（双端架构前置，14 列 + 2 索引，本地与服务器均验证建表）
- [x] **R11** 新增 4 个内容工具：`related_content`（四源聚合）/ `knowledge_lookup` / `recall_memory` / `web_search`；每源给出**可区分状态** `ok`/`no_match`/`empty`/`error`
- [x] **R11** 实现**可自证**的联网检索 `ai_service.glm_web_search`（必须带 `search_result:true` 才拿得到检索原文；拿不到就报失败，不返回正文）
- [x] **R11** 前端 `agent.html`：4 类结构化动作卡片（来源状态色块 / 可点来源链接 / 知识点标签 / 掌握度进度条），已做 XSS 与越界值防护
- [x] **R11** 自查拦下 **2 处我自己引入的风险**：默认超时 = 给所有写盘路径加硬取消；部署脚本 `Write-Output` + `return` 混用吞掉输出

## 已完成（R12–R14：下载入口 + 双端推理 + 备课）

- [x] **R12 下载入口刷新**：APK 重出为 **v1.4 / versionCode 5**（含 TTS 桥，`aapt` 验版 + `dex` 验特征）；桌面端 zip 重出；下载页的体积/日期改为**读取磁盘真实值**——原先页面写 v1.3/09-10，磁盘上其实是 **09-05** 的旧包
- [x] **R12 公开下载核验**：APK 远程 SHA256 与本地构建**逐字节一致**；页面显示真实 2.8 MB / 498 KB
- [x] **R13 锁拆分**（`services/session_locks.py`）：`fg_lock` / `file_lock` / `bg_lock` 三把；删除与改名**不再等待**在跑的回合（立刻 409）；`_save(guard_deleted=True)` 阻止"删了又回来"
- [x] **R14 后台推理端**（`services/background_agent.py`）：任务落库 + 执行循环 + 协作式取消 + **25 秒兜底硬杀** + 续跑 + **重启对账**；并发上限 5
- [x] **R14 备课**（`services/lesson_service.py` + `Lesson` 表）：可编辑课稿、**切片读取契约**、先建骨架再逐片填（取消后仍然可读）
- [x] **R14 前台工具**：`prepare_lesson` / `list_lessons` / `read_lesson` / `edit_lesson`（工具总数 23 -> **27**）；`prepare_lesson` 前台**秒回**、生成丢后台
- [x] **R14 前端**：后台任务卡片（进度 + 最近步骤 + **取消** + **接着做**）；取消说明如实写明"下一个检查点停下"
- [x] **R14 实测挖出 5 个缺陷**（含 2 个我自己引入的）：取消形同虚设（跑 120 秒不停）、进度恒为 0（`set_total` 从没调用）、兜底覆盖了更具体的原因、下载页版本号漂移、`gradlew` 在中文路径下失效

## 新功能待排（用户 2026-09-11 明确指正后）

- [x] **相关内容接入**（**必做，毋庸置疑**）——R11 完成
- [x] **双端实时**——R13 拆锁 + R14 后台推理端**已完成内核**（工具注册表 + 任务生命周期 + 协作取消 + 续跑 + 双线展示）；**P4 打断判定（cancel/keep/observe）未做**
- [x] **备课**——R14 完成（可编辑课稿 + 切片契约 + 两条触发入口）
- [ ] **临时上课**——**= AI 讲题的加强版**（复用 `lecture` 链路，不新建课程体系）；**未开始**
- [ ] **「提前录好」**——**「录」到底指预生成音频 / 预设讲解词 / 录屏录播，仍待用户确认**

## R14 遗留（下轮从这里接）

- [x] **✅ 已修（R17）：备课写不出内容的根因** —— 是**推理模型把预算烧在 reasoning 上**：
      `max_tokens=2048` 时 reasoning 吃满 2048、正文长度 0；`8192` 正常（reasoning 3336 + 正文 1404）。
      **而那个 2048 是我 R15 按错误直觉调出来的**。已修为 8192 + 重试下限 `REASONING_SAFE_MIN`，
      生产 E2E 验证两片均写出真实讲解词（1501 / 1097 字）
- [x] **✅ 已修（R17）：回归测试固化了"512 就够"的错误假设** → 改为按真实阈值断言
- [x] **✅ 已完成（R18）：知识体系与前置依赖**（206 知识点 / 211 前置边 / 3 学科）
- [x] **✅ 已完成（R18）：「今天该补什么」闭环 v1**（`review-plan`，按错题 + 掌握度 + 体系排序）
- [ ] **P4 打断判定**：把 cancel / keep / observe 三态并入意图分类
- [ ] **课稿页面**：现在能通过 Agent 对话读写 + 完整 HTTP API，但**没有独立页面**
- [ ] **P8 临时上课**
- [ ] **P6b** `agent_tools._src_questions` / `_src_notes` 依赖 `paper_service` 私有方法
- [ ] **P6c** `routers/system.py::_gen_quote` 的内联 `web_search` payload 与 `ai_service.glm_web_search` 重复，且**没带 `search_result:true`**，那次调用**无法自证**是否真的检索过
- [ ] **兜底硬杀的时序验证**：`Task.cancel()` 理论上可能落在写库中途（未构造出该时序）

## R18–R19：知识补漏骨架（2026-09-12）

- [x] 新增 `backend/data/curriculum_cn_junior.json`：初中**数学/物理/化学**知识点 + **前置依赖**（206 节点 / 211 边 / 难度分 4 档）
- [x] 新增 `services/curriculum_service.py`：加载 / **校验（错了就拒绝灌库）** / 幂等灌库 / 前置查询 / 补漏清单
- [x] `knowledge_graph.py`：新增**时序关系** `prerequisite`/`successor`、`merge_curriculum()`、`remove_curriculum()`、`get_prerequisites()`；**修边去重键**（原按 `(from,to)` 去重会吞掉第二种关系）
- [x] `routers/knowledge_graph.py`：新增 4 个端点（`seed-curriculum` / `curriculum` / `prerequisites` / `review-plan`，后两个支持 `grade` 筛选）
- [x] `agent_tools.py`：**修 `dict` 当 `list` 遍历的潜伏 bug**（图谱一有数据就会炸，空图谱时看不出来）+ **改掉写死的"图谱没有前置语义"话术** + 新增 `review_plan` 工具（工具总数 **28**）
- [x] 测试：`test_r17_agent_graph.py`（8 条，**刻意用非空图谱**）+ `test_r18_curriculum.py`（14 条）
- [x] 生产验证：服务器灌库 206 节点 / 211 边 / 12 簇；**重灌幂等**（`nodes_added:0, edges_added:0`）；`二次函数` 给出 **17 步**前置链

## 下一步（按用户确认的材料形态排）

- [ ] **1. 照片 OCR 管线真跑**（`/api/ocr/session/*` 已存在）——需要**用户走一遍真实流程**，我在旁边修坑
- [ ] **2. 真题/试卷 PDF 导入**：`pypdf` 已在依赖里；**扫描件抽不出文字**，需新增"提取内嵌图片 → 走现有 OCR"路径
- [ ] **3. 课本/教辅 PDF 按知识点切**：现在**有地方可切了**（R18 的体系），需做章节 → 知识点挂载
- [ ] **4. 自招难题分层**：`Question` **没有难度字段**（需加列 + 迁移），体系里已有 `自招` 档（4 个知识点）

## 长期待办（需外部条件）

- [ ] **浏览器 / 真机冒烟**：含 **Android WebView 是否提供 `webkitSpeechRecognition`** 这个决定性未知
- [ ] **`desktop/` 在 Windows 实跑**（zip 已用当前源码重出，但未实跑）


## R15–R16 收尾修复

- [x] **R15 步骤状态不再谎报**：`tick()` 记 `running`（原先写死 `done`），下一步开始时把上一条标 `done`，终态时收尾（`_finalize_steps`）。生产实测已确认进度正常推进（50 -> 75 -> 95 -> 100）、步骤显示 `running`
- [x] **R15 单片调用加 120 秒上限** + 目标长度 6000 -> 1500 字、tokens 4096 -> 2048
- [x] **R16 `logger` 标准流切 UTF-8**（`_utf8_stream`）：中文 Windows 上 stdout/stderr 默认 GBK，含 `²` 这类字符的日志会被 logging 静默吞掉。**这是防丢日志，不是修致命 bug** —— 实测确认旧路径不抛异常（见 FreqErr 里的严重程度说明）
- [x] **推翻我自己的一条错误结论**：我一度判定"GBK 编码导致 `deepseek_chat` 崩溃"，实为**我的诊断探针把 `print` 写在 `try:` 里**、自己抛的 `UnicodeEncodeError` 被我记到了被测代码头上。已按真实原因改写 FreqErr，并新增一条教训条目



## 长期待办（需外部条件，本机无法推进）

- [ ] **浏览器 / 真机冒烟**：打包下载 · 编辑器点击加件与批量编辑 · 对比模式全屏 · 离线提示 · mermaid 渲染时序 · 触屏回归 · **Agent 页 4 类新卡片在真机的表现**
  —— 其中 **Android WebView 是否提供 `webkitSpeechRecognition`** 是决定性未知（权限 6 项已查实齐备，唯独此 API 有无无法在本机验证）
- [ ] **`desktop/` 重新出包**（本轮改了 4 个桌面端文件）
- [ ] **APK 重出**（后端与模板均有改动）

## 已知未做的小项（无害，记录备查）

- [ ] 桌面端 `set_click_through` / `open_page|close_page` 无生产接线（**`open_page` 是用户实测后主动否决的方案，不擅自接回**）
- [ ] `ClassroomSync._lock` 定义后从未 acquire（防重入实际靠无锁布尔 `_syncing`）
- [ ] `ClassroomSync._coach` 参数零读取（补讲数据经 `teach_logger` 回调注入）
- [ ] `test_r60b_board.py` 的 `svg_ok` 断言与 R6 的 `ref_ok` 拆分后不一致（该脚本已被 `collect_ignore` 排除、不在回归内）
- [ ] `screen_paint.py` 的 `QTimer` 导入未使用

## R23：按用户九大重点的检修（2026-09-12）

> 详见 `updates/20260912_检修03_九大重点_R23.md`。基线 230 → **258 passed**；桌面端新回归 24/24。

- [x] **最重发现**：两个「实现完整 + 有单测 + **生产零调用点**」的功能 ——
      `request_teaching`（学生随时打断讲课）与 `_validate_svg_quality`（SVG 质量自检）。
      单测直接调函数、永远不需要调用点，所以它在"全绿"仪表盘上完全隐身。
- [x] **桌面端「随时提问」全链**：Ctrl+Alt+A 全局热键 → 置顶输入框 → `ask_now` → 既有
      卡片板书 + 流式讲解 + TTS；托盘菜单同入口；**与音频感知解耦**（音频关掉也能问）；
      闸门被拦时给中文理由（静音/冷却）
- [x] **SVG 质量自检接线**：挂到统一落盘口 `_write_svg`，结果经 `/api/diagram/check` 暴露；
      **只观测不拒收**（数学图黑白灰判据会误杀彩色实验图），新增 `_is_experiment_svg()`
- [x] **后台任务完成 → 召回 Agent 继续讲解**：新工具 `task_recall` + `[系统召回]` 会话旁路
      （不过分类器，零误路由）+ 前端自动召回一次、跨会话不误召、聊天区可见召回芯片
- [x] **鉴权韧性**：token 落 localStorage + 401 补密重试**一次**（原来改密码＝全站 401 且无入口）
- [x] **题 → 笔记入口**：题库桌面行 + 移动卡片「存笔记」（`[[QUESTION:id]]` + 题干 + 答案，
      确定性搬运不烧额度）+ 笔记页 `?note=<id>` 深链
- [x] **讲课页边看边问**：提问带当前步骤上下文、走 `/api/chat`、不打断朗读
- [x] **课稿离线**：`/api/lessons` 进离线 GET 白名单（原来"提前备好的课"离线直接看不到）
- [x] **安卓端三修**：TTS 长文本分片（4000 字符上限静默截断）、TTS 初始化竞态排队（丢首句）、
      讲课屏幕常亮；`versionCode 5 → 6`
- [x] **确认可靠（读码，未改）**：排版审查链路（视觉优先 + 覆盖守卫 + 不可用给用户可见告警）、
      题库/搜索路径安全、工具表驱动、后台任务协作取消/兜底/重启对账
- [ ] **APK 重出 + 桌面端重打包**（本轮改了安卓 2 文件、桌面 4 文件、后端若干）——
      `gradlew` 中文路径失效，需 ASCII 路径构建
- [ ] **安卓语音提问**：WebView 无 Web Speech API → 要么接原生 `SpeechRecognizer` 桥，
      要么按 UA 隐藏语音入口（现状：按钮在、点了没反应）
- [ ] **设置页改密码入口**（现在只能手改服务器 settings.json）
- [ ] **三端功能对齐矩阵**：22 个网页页面 × Android × 桌面，标「直接用/残废/无入口」
- [ ] **召回在页面关闭时缺失**：任务在页面关着时完成 → 下次打开看不到"召回"这一动作
      （可选：wake 标记落库）

## R24：三端对齐 / 安卓语音 / 视觉检查加深 / 出包（2026-09-12）

> 详见 `updates/20260912_检修04_三端与语音_R24.md`。基线 258 → **278 passed**；APK v1.5(6) 已出；已部署。

- [x] **三端功能对齐矩阵**（22 页面 × 安卓 × 桌面，逐行代码证据）→ `updates/20260912_三端功能对齐矩阵_R24.md`
- [x] **安卓原生语音识别桥**：`SRBridge`（SpeechRecognizer + 轮询事件队列 + 权限回调 + 连续错误上限）
      + JS 适配层 `android_sr.js`（与 Web Speech API 同形）→ focus 模式语音作答在 App 内可用
- [x] **专注模式补原生 TTS 桥**（此前 App 内完全静音）
- [x] **复制按钮三端修复**：`navigator.clipboard` 需安全上下文，http://IP 下原写法同步抛错、按钮无反应
      → 全局 `$copyText`（execCommand 回退），5 处接入
- [x] **笔记 ← 题目 有去有回**：建笔记补 `references` 登记（否则正文是裸标记）+ 笔记「题目」栏显示关联题库题目
- [x] **编辑器保存后自动质量自检**（消费 R23 接线的 `/api/diagram/check`）
- [x] **下载页版本号从 build.gradle.kts 读取**（R12 只修了体积/日期，版本号又漂移了）
- [x] **前端静态巡检器** `backend/audit_layout.py`（5 条规则）+ 进回归 + 正反例自证测试
- [x] **推翻旧结论**：APK 构建失败**不是**中文路径导致（ASCII 路径同样抛 ClassNotFoundException）——
      真因是 gradlew wrapper 失效；改用本机 gradle 8.2 发行版的 `gradle.bat` 直接构建成功
- [x] **APK v1.5 / versionCode 6 重出**（dex 内符号核验：AndroidSR/pollEvents/SRBridge/splitForTts 等）
- [x] **桌面包重出**：`学习搭子-桌面端-20260912.zip`（97 条目 / testzip OK）
- [x] **部署到服务器并端到端核验**（含"发 `[系统召回]` 消息 → 返回 `task_recall`"这样的真调用探针）
- [ ] **真机验证**（本机无法替代）：触屏拖拽编辑器 / 语音识别实机 / 长稿朗读 / 断网看课稿
- [ ] 原生通知桥（任务完成提醒）；网页端屏幕常亮（需 https 才能用 Wake Lock）
- [ ] 设置页改密码入口（现仍手改服务器 settings.json）

## R27：深检（2026-09-12）

- [x] 智能上传题库：补 multi_images + staged + original 双写（对齐 OCR 管线）
- [x] 智能上传试卷页：真建 Question + 挂 session.question_ids（原先目录无人读）
- [x] P4 打断：仅 cancel 旁路；keep 不再吞「继续讲…」类提问
- [x] **试卷 PDF 导入**（文字版）+ 整卷页 UI；扫描版诚实失败
- [ ] APK 重出 + 部署
- [ ] OCR 后自动难度标注
- [ ] 临时上课（lecture 加强版）
- [ ] 扫描版 PDF 抽图 OCR（FUTURE 0b）

## R26：智能上传 / Agent 文字能力 / 小组件（2026-09-12）

- [x] **智能上传**：混页（题/笔记/试卷）启发式 + 视觉分类分流；`/api/smart-upload`；整卷页 UI
- [x] **Agent 文字能力测试**：注册表/确认闸/别名/召回旁路/打断/处理器冒烟（`test_r26_agent_smart.py`）
- [x] **首页小组件**：`smart_upload`、`lessons`
- [x] **安卓桌面小组件**：`LearningWidgetProvider` + 深链安全（仅站内路径）；versionCode 7 / 1.6
- [ ] APK 重出 + 部署（本轮未推服务器）
- [ ] 智能上传视觉分类生产抽检

## R25：设置改密 / 课稿页 / 难度 / 搜题→笔记 / P4 打断（2026-09-12）

- [x] 设置页改密码（验当前密码；env 密优先时拒绝并说明）
- [x] 课稿独立页 `/lessons`（列表/编辑/定稿/缓存离线）
- [x] Question.difficulty + 迁移 + 筛选 + 详情保存
- [x] 搜题结果「存笔记」+ OCR 文本存笔记
- [x] P4 打断：有运行中任务时确定性 cancel/keep

## 部署备忘（本地 → 服务器）

- 目标 `C:\all_projects\learningAgent\backend\`；nssm 服务 `LearningAgent`（8000）
- 通道 `ssh mindog`（8.138.12.209 / Administrator）
- 脚本：`_deploy_r5.ps1`（20 文件）· `_deploy_r5b.ps1`（5）· `_deploy_r6.ps1`（4）· `_deploy_r7.ps1`（4）· `_deploy_r8.ps1`（4）· `_deploy_r9.ps1`（1）· `_deploy_r10.ps1`（2）
- 远端：`_remote_deploy.ps1`（幂等备份 → 强杀 → 自动拉起 → 核验）· `_remote_verify.ps1`（只核验）
- **三条铁律**：① `.ps1` 含中文**必须带 UTF-8 BOM**（与 `.py`/`.js` 相反）；② 远端调 nssm **必须 `cmd /c`**；③ **永不用 `nssm restart`/`stop`**（会把服务打停），改强杀 + 自动拉起
