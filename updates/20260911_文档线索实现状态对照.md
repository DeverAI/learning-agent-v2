# 文档线索 → 实现状态对照（2026-09-11 R5）

> 触发：「你看一下所有文档中含有的所有线索代表的优秀功能都实现了没，补好再查，然后推到服务器」
> 方法：**两路只读子 Agent 穷尽挖掘**（核心文档 / 技术文档与历史轮次）+ 逐条代码对照 + 本轮可落地项实施
> 前序：`updates/20260911_deep_audit_r4_工作记录.md`（上一轮：三路代码审计 + 16 项修复）

## 0. 挖掘结论概览

| 来源 | 提取条目 | 其中「未做/待做」 |
|------|----------|-------------------|
| `README.md` | 111 条功能/配置/端点声明 | 0（全部为已声明能力） |
| `Design.md` | 200+ 条（§1–§18 全文） | **13**（§15 当前限制 + §13.2 备注） |
| `FUTURE.md` | 36 条 | **36**（10 未实现需求 + 2 待处理 SVG + 14 优化方向，及子项） |
| `Techniques.md` / `FreqErr.md` / `todo.md` / `done.md` / `updates/*` | 见下节 | 见下节 |

**两路挖掘各主动暴露了一处文档矛盾**，均在本轮修正（见 §3）。

## 1. 本轮补实现（4 项，全部验证）

| # | 来源线索 | 问题 | 实现 | 验证 |
|---|----------|------|------|------|
| 1 | `FUTURE.md:43` | 前端在同一次点击里**同步开最多 4 个 `window.open`**（`paper_detail.html` 的 `dlCustom`），浏览器只放行第一个、其余被静默拦截，提示却写「已为您分 4 个文件下载」——**用户只拿到 1 份却被告知成功** | 新增 `GET /api/papers/{id}/download-bundle?modes=...` 返回单个 zip；多选时前端改用 `<a download>` 单次触发，单选保持原路径不变 | 编译 OK；OpenAPI 路径表确认新老端点并存（193 条路由）；回归 160 passed |
| 2 | `FUTURE.md:45` | 搜题 `_find_matches` **全表加载** + 逐条 `SequenceMatcher`（**同步**计算）→ 题库越大越阻塞事件循环，会拖慢**所有**接口 | 题库 > 1000 条时先用学科/年级做 SQL 前置过滤（过滤后为空**回退**到不限科级的最近 N 条以保召回），并给候选集加 1200 条硬上限 | 回归 160 passed |
| 3 | `FUTURE.md:47` | 三处上传（ocr / correction / search）**先全量 `read()` 再判大小**——判定只是事后拒绝，内存峰值已经发生 | 抽 `backend/services/upload_guard.py` 的 `read_upload_limited()`，三处共用「分块读 256KB + 累计上限 + 超限立即 413」 | 编译 OK；回归 160 passed |
| 4 | `FUTURE.md:42` | 编辑器调色板只能 HTML5 拖拽加元件，触屏（Android WebView 无 DnD）与键盘完全加不上 | 调色板补 click / Enter 兜底（落在画布中心，与 drop 同款偏移） | 回归 160 passed（上一轮 R4 完成，本轮在 FUTURE.md 标记） |

> 实现原则：**同一规则不写多份**（`upload_guard` 抽共享模块），并对改动做**回退兜底**（`_find_matches` 前置过滤为空时回退），避免"修一个坏一个"。

## 2. 仍未实现清单（已定位，按可落地性分类）

### 2.1 小改可落地（未做，本轮时间不足）

| 来源 | 条目 |
|------|------|
| `FUTURE.md:37` | 温度计刻度线超出组件宽度的视觉溢出修复 |
| `FUTURE.md:38` | 锥形瓶液体计算公式验证 |
| `FUTURE.md:39` | 自动 viewBox 对连接线和刻度溢出的覆盖 |
| `FUTURE.md:44` | `--text3` 精确对比度测算，必要时同步提亮 `--text2` |
| `FUTURE.md:46` | 同类全表物化点按 `_find_matches` 同款模式继续排查 |
| `FUTURE.md:33/34` | 烧杯(#4) viewBox 880×532 重制；各种夹(#12) 拆分简化渐变与 `feGaussianBlur` |
| `Design.md:169` | 部分实体仍以字段组合表示状态，未全部落成数据库枚举 |
| `Design.md:296` | PNG 导出 |

### 2.2 中等特性（有明确设计方案，工作量天级）

| 来源 | 条目 | 备注 |
|------|------|------|
| `FUTURE.md:17-26` | **尺规作图与方格题绘图** | 方案 B 基础组件已实现（`compass_arc`/`ruler_line`/`equal_length_mark`/`perpendicular_mark`/`grid_paper` 已注册并在调色板显示）；**AI 步骤 JSON 驱动与状态机仍未实现**。文档已写明安全要求（动作白名单、坐标范围、最大步数、HTML escape、走 `_sanitize_svg`）与路径校验（`^[a-zA-Z0-9_-]{1,64}$`） |
| `FUTURE.md:12/13/14` | 前端拖拽实时端口对齐 / 双向接口映射优化 / 元件真实 SVG 缩放对齐 | 编辑器画布三件套，互相耦合 |
| `FUTURE.md:15` | 批量选择后批量属性编辑 | 当前只显示选中第一个的属性 |
| `Design.md:280` + `FUTURE.md:40` | 自动分题区域坐标裁剪 | 当前可能复用整页原图 |
| `FUTURE.md:11` | schemdraw 集成（电路图） | 新依赖；用户曾确认"电路先预设这个" |

### 2.3 大特性 / 架构级（周级）

| 来源 | 条目 |
|------|------|
| `FUTURE.md:10` | **小米 MiMo 音频输入理解**（脱离浏览器 Web Speech API 做 ASR）。注：R4 已确认 MiMo 平台**确有** `mimo-v2.5-asr` 与多模态 audio input，**平台能力不再是未知项**，只差接入 |
| `FUTURE.md:28` | 本地向量索引语义相似题检索（基于 SQLite + Python） |
| `FUTURE.md:27` | 题库管理权限与一体化题目工作台（老师/学生角色边界 + 连续工作台） |
| `FUTURE.md:29` | AI 解题过程置信度可视化标注 |
| `FUTURE.md:30` | 教师批注笔迹矢量化复用为模板 |
| `Design.md:285/286` | 语音情感专用模型；基于记忆网络图结构的智能遗忘 |
| `Design.md:358` | 多用户权限、跨设备同步、并发写入隔离；临时产物自动清理策略；外键级联与迁移体系 |

### 2.4 部署 / 设备类（需环境或签名材料）

| 来源 | 条目 | 卡点 |
|------|------|------|
| `FUTURE.md:48` / `Design.md:326` | 服务端 HTTPS（自签证书 + `networkSecurityConfig` 信任锚） | 需部署变更；当前是"局域网明文 http"的**已知取舍** |
| `FUTURE.md:49` | APK release 签名 + minify | **需 keystore**，非代码问题 |
| `FUTURE.md:50` | WebView `onSaveInstanceState`/`restoreState` 进程死亡恢复 | Android 侧（`android/` 目录存在） |
| `FUTURE.md:41` / `Design.md:290` | 真实触屏设备持续回归（相机上传 / 单指平移 / 双指缩放 / 安全区 / 390px 窄窗） | **需真机**，本环境无浏览器无设备 |

## 3. 本轮修正的文档漂移（3 处）

| # | 位置 | 矛盾 | 处理 |
|---|------|------|------|
| 1 | `README.md:216` ↔ `Design.md:51-63` | README 写「本项目**只包含 WEB 服务端**，无桌面端组件」；Design 写「系统包含 WEB 服务端与桌面端两个组件」 | **桌面端已于 2026-09-09 并入 `desktop/`，README 未同步**。已改写为现状，并新增 §15.3 桌面端章节（含"独立副本、改动不自动传导"的同步纪律） |
| 2 | `FUTURE.md:5` ↔ `FUTURE.md:16` | 同一文件内，首节写「单题批改：**已实现**」，未实现清单第 7 条又列「单题批改」 | **同一份文档自相矛盾**。按首节为准，已把第 7 条标记为陈旧条目并说明 |
| 3 | `FUTURE.md` 优化方向 4 条 | 声称"待改"，实际已完成 | 已就地标记为「已完成（2026-09-11）」并附实现要点 |

## 4. 诚实声明

- **本轮只补了 4 项**（明细见 §1）。§2 的其余条目**均未实现**，其中 2.3/2.4 属周级或需外部条件，**不适合在单次改动里赶工**——贸然动手大概率产出"看着有、实际不能用"的东西，与项目「容量诚实：不伪造成功」的原则冲突。
- 挖掘口径：子 Agent 只读挖掘 + 本人代码对照。**"已声明功能"未逐条实测**（README 111 条全部标为"已声明"），因此 §2 清单**可能仍有遗漏**——尤其是 `Techniques.md`/`FreqErr.md`/`updates/*` 里以"应然机制"表述的条目，需与代码逐条比对才能定论。
- 前端类改动（打包下载、编辑器兜底）**只做了静态验证 + 回归**，本环境无浏览器，未做真机冒烟。

## 5. 部署结果（2026-09-11 18:38）

**部署脚本**：`backups/deploy_r5_20260911/_deploy_r5.ps1`（本地编排）+ `_remote_r5.ps1`（远端执行），均已带 UTF-8 BOM 并做过语法自检。

| 步骤 | 结果 |
|------|------|
| 服务器端备份 | `C:\all_projects\learningAgent\_backup_20260911_r5\`（services / routers / static / templates 四个目录） |
| scp 推送 | **20 文件，0 失败** |
| 服务重启 | `nssm restart LearningAgent` → **SERVICE_RUNNING** |
| 端点核验 | `/api/health`、`/download`、`/papers`、`/search`、`/questions`、`/notes` **全部 200** |
| 新端点注册 | openapi 含 `download-bundle` = **True** |
| 新端点行为 | 伪造 paper id → **404**（证明路由已加载并走到自身校验，而非静态兜底 404） |
| 新模块就位 | `backend/services/upload_guard.py` = **True** |

**未部署**：文档（`README.md` / `FUTURE.md` / 记录）与测试文件——服务器上只跑 `backend/` 代码，文档不入运行路径。**刻意未动**：`backend/settings.json`、`storage/`、`data/`、`user_profile.json`（服务器侧密钥与数据）。

**部署过程踩到 4 个坑，已全部录入 FreqErr**：`.ps1` 必须带 BOM（与 `.py`/`.js` 相反）、`-Raw` 返回数组致 `.Trim()` 抛错、**`nssm` 不在 PowerShell PATH 导致"文件已更新但服务未重启"的静默假成功**、部署备份非幂等会毁掉改动前快照。其中第三条最危险：端点全 200 却跑的是旧代码，靠"新端点对伪造 id 返回 404"这一判据才发现。

## 6. 追加批次：中等 + 小改（2026-09-11 晚，用户指定「大特性与部署/设备类不做」）

### 6.1 已完成（7 项，全部有探针或回归验证）

| # | 线索来源 | 实测问题 | 处理与验证 |
|---|----------|----------|------------|
| 1 | `FUTURE.md` 优化方向（全表物化点） | — | **排查结论：干净**。`questions.py:1396` 已是「JSON 列 LIKE 定向查询」，`search.py:331` 是本轮新加且带 limit。**未发现新的全表物化点** |
| 2 | `FUTURE.md` 优化方向（`--text3` 对比度） | 浅色主题 `--text3(#9b9ca5)` 对页面底仅 **2.52:1**、卡片底 2.73:1，**连 AA 大字线(3.0)都不到**；深色 4.17:1 也不足 4.5。而该变量承载 112 处 `hint`(12px)/`sys-time`(11px)/空状态等**信息性正文** | 浅色改 `#6f7077`（bg 4.56 / card 4.93），深色改 `#80808b`（4.53 / 4.93）；`--text2` 无需调整，层次 text > text2 > text3 保持。探针 `_probe_contrast.py` |
| 3 | `FUTURE.md`（温度计刻度溢出） | 后端 assembler **读** `overflow` 元数据，但 `available_components()` **不下发** → 编辑器全程只按 `w/h` 画命中区与选择框，**看到的内容与能点到的框对不上、并压到邻居** | ①`available_components()` 补 `overflow`；②编辑器命中区与选择框按 `overflow` 扩边（未声明者为 0，行为不变）。探针 `_probe_overflow_viewbox.py` |
| 4 | `FUTURE.md`（自动 viewBox 覆盖连接线与刻度溢出） | `min_x = max(0, min(all_x) - 20)` —— 内容靠近原点时**这 20px 留白被整个吃掉**，左/上侧的刻度与标注正好落在 viewBox 外被裁 | 去掉 `max(0, ...)` 钳制（SVG 允许负原点，下游只读宽高）。验证：温度计在 (5,5) 时 viewBox 起点由 `0` → **`-15`**；负坐标连接线端点由被裁 → **`-50 -40` 已覆盖** |
| 5 | `FUTURE.md`（锥形瓶液体计算公式验证） | `_render_liquid_conical` 的锥度**插反**：`ratio = 1.0 - (ly/h)` → 液少（贴瓶底）算出接近瓶口的**最窄**液面，快满（在细颈）算出接近瓶底的**最宽**液面，锥形瓶读成直筒 | 改为 `ratio = ly / h`。探针 `_probe_conical.py`：level 0.1→液面 91% 宽、0.9→26% 宽，单调递减 ✓ |
| 6 | `FUTURE.md`（批量选择后批量属性编辑） | **写入端早已支持批量**（`updateProp` 是 `selectedIndices.forEach`），但**显示端只取第一个选中元件的值**填进输入框 → 「面板显示 A 的值、一改却把 B/C/D 全改了」，用户看不到真实状态 | 多选时按**公共值**显示，取值不一致则留空 + 占位「多个值」，并在面板顶部加批量编辑提示。`node --check` 通过 |
| 7 | `FUTURE.md`（元件真实 SVG 的缩放对齐） | 逐组件探针量化：43 个组件里**只有 `conical_flask` 一个**错位——声明 40×98（比 0.408）而内部真实 SVG 宽高比 **1.653**，偏差 **305%**；`preserveAspectRatio` 令图形垂直居中，端口打在两端全偏 | 保持宽度 40、高度对齐到图形比例取 **24**，端口同步 `dy=24` → 复验偏差 **0.8%（对齐）**。其余 11 个真实 SVG 组件 ≤4.3%，30 个程序化组件端口基准天然成立 |

### 6.2 未完成（3 项，本轮明确不做）

| # | 线索 | 不做原因 |
|---|------|----------|
| 1 | 前端拖拽实时端口对齐（吸附） | **FUTURE.md 的前提不成立**：全仓 grep 无「端口绿点」渲染，端口目前只在「语义组合套用」时用于自动摆放。即这是**从零做的新交互**（渲染端口点 + 拖拽吸附 + 阈值与多选语义），非接线工作；且本环境**无浏览器**无法验证交互 |
| 2 | 双向接口映射优化（悬空接口 vs 底部接口差异化） | 需求描述本身是领域语义（玻璃棒/烧杯悬空口 vs 铁架台底部口），缺少可判定的验收标准，需先与用户对齐口径 |
| 3 | 自动分题区域坐标裁剪 | 需要版面分析（定位每道题在整页图中的区域），FUTURE.md 亦自述「尚无通用区域坐标裁剪」，属新算法而非调整 |

### 6.3 部署与一次生产中断（已恢复并入库）

第二批 5 文件推送成功，但 `nssm restart` **把服务打停**（STOP 卡在 `SERVICE_STOP_PENDING`，START 未执行）→ 7 个端点全部 ERR。**手动 `nssm start` 约 1 分钟后恢复**。

- 部署脚本已重写为 **`stop` → 轮询至 STOPPED → `start` → 轮询至 RUNNING**，永不使用 `restart`
- 新增独立核验脚本 `_remote_verify.ps1`（只核验、必要时拉起，绝不重启）
- 最终核验：**SERVICE_RUNNING**；7 个端点全 200；`download-bundle` 注册 True；`/api/gallery/components` 含 `overflow` True；5 文件 mtime 18:55；`theme.css` 含新 `--text3` True
- 两条新 FreqErr：`[nssm restart 会把服务打停]`、`[部署核验必须验「改动生效」]`（现共 **137** 条）
