# 2026-09-13 R35：LLM 全量跑测 + 第二轮复检残留

## 1. LLM 测试套装（真 API，非 mock）

服务器 `/all_projects/_llm_full.py`，结果 **PASS=8 FAIL=0**：

| 项 | 证据 |
|---|---|
| deepseek_chat | OK，2s |
| light_task_chat | `{"ok":1}` |
| deepseek_solve | 完整字段，9s |
| diagram | 磁盘 1044B SVG |
| vision_mimo | 「红色」 |
| glm_web_search | 7805 字 |
| lesson_generate | 2/2 片，26s |
| pdf_remap_db | pua=0，`2y x x a x= - -` |

Git / 服务器哈希：**一致**（第二轮核对）。

## 2. 第二轮复检抓到的残留（第一轮以为改完了）

| 残留 | 处置 |
|---|---|
| 侧栏 **Agent/笔记 各出现 2 次** | 去重，现 notes=1 agent=1 lecture=1 |
| 大量 `e.message` 直接拼 UI | 批量改 `$errText(e)`（questions/agent/lecture/search/batch/dashboard/app） |
| 质疑区 `textContent` | 已改 `$md` |
| Agent 无附件 | 已加附件按钮 → 智能上传 |
| 暗色黑板 SVG | `board-svg-card` 浅底 + 禁反相 |
| 讲课黑板 | 全屏高度 + 暂停按钮 |

## 3. 仍未闭环（如实）

- 4 道 PDF 题仍在 `generating_solution`（重映射后重试中）
- `circ` 具体指什么需你截图（可能是 SVG 圆在暗色或 KaTeX `\circ`）
- 视觉检查画布系统：端口 dist=0 已修，**真机/截图 QA 仍缺**

## 4. 回归

本地 **340 passed**；线上 health **200**；`auto-render` / `errText` / `agentFile` / `lecBoard` / `pause` 均在页面上。

Git：`3e796d1` 已推。
