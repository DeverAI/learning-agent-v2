# 2026-09-13 R34：PDF 数学乱码根因 + 多源摄入 + 侧边栏

## 1. 为什么 AI「一顿质疑」（PDF 核对结论）

用户文件：`数学自招晚课第1讲a（60份）(1).pdf`（4 页，自招数学讲义）

pypdf 抽出的是 **Adobe Symbol 私有区**（U+F02B=`+`、U+F03D=`=`、U+F06F=`°`…）：

```
2y x x a x     ← 原抽字
2y x x a x= - -    ← 重映射后
```

库里 4 道题与该 PDF 对应（页 1/3/4 的 1、17、20 题），`question_challenge_high` 理由就是「OCR 缺符号」——**质疑是对的，根因是抽字坏了**。

## 2. 修复（用户要求：多源丢给 AI，别只靠 pypdf）

| 组件 | 作用 |
|---|---|
| `pdf_math_remap.py` | Symbol PUA → Unicode |
| `pdf_ingest.py` | pypdf 重映射 + **pypdfium2 页图渲染** + **MiMo 视觉 OCR** + 合并 |
| 合并策略 | 视觉优先；pypdf 作交叉；都差时**明说还原题意，禁止因乱码直接质疑** |
| 页图 | 落盘为 `original.png`，解题可当原题图 |

已对库里 8 道 pdf_page **就地重映射**（PUA 37→0 等）。

## 3. MD / 侧边栏

- **KaTeX 缺 `auto-render.min.js`** → `$md` 里 `renderMathInElement` 永远不存在 → 公式不渲染。已补 script。
- **侧边栏**：讲课/课稿/智能上传/笔记提到一级；管理只留设置/诊断等。

## 4. 回归

本地 **340 passed**；服务器 health **200**；`/dl/android` MIME 已是 `application/vnd.android.package-archive`。

## 5. 以后上传 PDF

自动走多源摄入。扫描版也可（页图 OCR）。不要再只靠 pypdf。
