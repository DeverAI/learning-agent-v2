# 2026-09-13 R28：部署上线（智能上传全套）+ 密钥 + 看门狗/自启 + 跨仓信

## 0. 结果一览

| 项 | 状态 |
|---|---|
| 线上 | http://8.138.12.209:8000/ · health **200** |
| LearningAgent | **RUNNING** · AUTO_START · AppThrottle 15s · AppRestartDelay 8s |
| LearningWatchdog | **RUNNING** · AUTO_START · 60s 探活 |
| 主模型 | `deepseek-v4-flash` |
| 已配密钥 | DeepSeek / 智谱 / 小米 token-plan（**不入库、不写日志、临时文件用完即删**） |
| 端点核验 | `/api/smart-upload/destinations` `/lessons` `/batch-upload` `/settings` `/api/home-widgets` `/download` 全 **200** |
| 公网 | `http://8.138.12.209:8000/api/health` **200** |
| Git | 本地 commit `60036bc`；**push 失败**（本机连不上 github.com:443，Connection reset / timeout）——需网络恢复后重推 |

## 1. 部署了什么

- 智能上传（混页分流）+ 整卷页 UI + PDF 文字版导入  
- 课稿页 `/lessons`、设置改密、题目难度、搜题→笔记、P4 打断修复  
- 全量 backend 源码（含此前缺失的 `routers/feed.py` 等 —— **见 §3 故障**）  
- `ops/watchdog_learningagent.py`（nssm 绝对路径补丁）

## 2. 密钥（脱敏）

- 写入服务器 `backend/settings.json` 后**立即删除**临时 `_la_keys.json`  
- 核验只输出 `HAS_KEYS=deepseek,zhipu,xiaomi`，不打印明文  
- 本机与仓库 **无** settings.json / All_Apis 副本入库  

## 3. 部署踩坑（已记 FreqErr 精神）

1. **缺 `routers/feed` → ImportError → 进程秒退 → nssm 判短命并 PAUSED**  
   - 表象像「服务被暂停」，根因是 import 失败  
   - 处置：全量 scp backend 源码后 `nssm start`  
2. **看门狗裸调 `nssm`**：服务账户 PATH 无 nssm → WinError 2 永远拉不活  
   - 处置：改为绝对路径 `C:\dsh\nssm.exe`（本地仓库已修）  
3. **铁律**：全程未对 LearningAgent 使用 `nssm restart/stop`；重建服务时用 remove+install，日常换代码用杀 uvicorn + 自动拉起  

## 4. 跨仓信

- 回文：`自招学习/docs/letter_from_learningAgent_new_R28.md`  
- 收文归档：`学习Agent_new/updates/20260912_自招系统共享调用告知.md`  

## 5. 待办

- [ ] 网络通后 `git push origin main`（commit 已在本地）  
- [ ] APK 重出（安卓小组件 versionCode 7 尚未打包上传）  
- [ ] 真机：智能上传分类 / 课稿离线 / 语音  
- [ ] OCR 自动难度、临时上课  
