# 2026-09-14 R39：问题反馈界面 + 30 分钟巡检

## 1. 问题反馈界面

- 页面：`/feedback`（手机优先，大按钮）
- API：
  - `POST /api/feedback`（kind/question/paper/feature/other + message + qid/contact）
  - `GET /api/feedback`
  - `POST /api/feedback/{id}/done`
- 存储：`storage/feedback.json`（仿 system_messages，加载失败拒绝写回）
- 提交后自动写一条首页系统消息

## 2. 30 分钟巡检

- 启动：lifespan 起 `_health_patrol_loop`，每 30 分钟
- 检查：题库 `status=error` 数量 + 近 1 小时新错 + 未处理反馈
- 有问题 → 写 system_messages（首页消息栏）
- 手动：`GET /api/health/patrol`

**线上首跑结果**：发现 8 道 error 题 + 近 1 小时新错（`Image not found`）

## 3. 三端同步

- 反馈页走标准 base.html，网页/安卓 WebView/桌面 WebView 都可用
- 巡检结果进 system_messages，三端首页消息栏都能看到
- 桌面端可后续加「问题反馈」托盘菜单（当前走网页）

## 4. 部署

- 本地 **340 + 5** passed
- 线上：`/feedback` **200**、`/api/health/patrol` **200**
- Git 待推
