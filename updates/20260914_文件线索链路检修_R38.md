# 2026-09-14 R38：文件线索访问链检修

## 访问链（先查什么 → 到哪）

```
batch_upload.html
  → POST /api/smart-upload(/classify)
    → routers/smart_upload.py
      → services/smart_upload_service.py
        → pdf_ingest.py / ocr_service.process_image / route_text_*

agent.html
  → POST /api/sessions/{sid}/chat
    → routers/sessions.py
      → agent_core.REGISTRY
        → agent_tools.py (30 tools)

lecture.html
  → lecture.js askAboutStep
    → **原先 /api/chat** ← 发现问题
    → 改为 /api/sessions/{sid}/chat

questions.js
  → 存笔记 → /api/notes + /references
  → 质疑区 $md

app.js
  → $errText / $md / marked.parse
```

## 链上抓到的真问题（已修）

**讲课页「边看边问」走旧 `/api/chat`**：那是另一套分类器，**没有**课稿/导入/补漏等工具。
学生在讲课页问「帮我备课」得到弱回答。

**修**：`lecture.js` 改为创建/复用讲课会话 → `/api/sessions/{sid}/chat`（完整工具表）。
服务器已核验：`has_sessions_chat=True`、`has_ensureLec=True`。

## 顺带确认（链上通过）

- `h_add_question` 不再要求用户必填答案（哈希一致）
- `h_save_image` 已写 multi_images（哈希一致）
- notes references 契约完整
- password_guard 对 `/api/*` 生效，页面/静态豁免

## 数据保全

- 改前备份：`backups/audit_r38_20260914/`
- Git：`e0cab28` 已推（含 R36b/R37/R38）
- 服务器：health **200**，关键文件哈希与本地一致

## 状态

- 本地 **340 passed**
- 线上 health **200**
- Git 同步
