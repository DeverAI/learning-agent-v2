# 2026-09-14 R37：逐文件深检 + 全套 LLM 实测（真 API）

## 1. 逐文件翻找（不是纸面）

本轮逐段读了 `agent_tools.py` 关键处理器、`ocr_service.py`、`pdf_ingest.py`、`calc_service.py` 等。

### 翻出并修掉的真缺陷

| 位置 | 问题 | 修 |
|---|---|---|
| `h_add_question` | `if not answer`：用户不给答案时**整题不写库**（模型明明能推导） | 改为只要 `q_html`+`a_html` 完整即可 |
| `h_add_question` | `max_tokens=32768` 写死 | 改走 `ds_max_tokens` |
| `h_save_image` | 不写 `multi_images` → 解题链退化 | 补 multi_images + 传入 process_image |
| `h_import_and_prep` | `make_paper` 只提示「告诉我确认」，**从不组卷** | 真正调 `generate_paper` |
| `calc_service` | `a; b` 多表达式 SyntaxError（R36） | 按 `;` 拆 + prev_result 链 |
| `ocr_service` | 4 题并发写锁 + 审计失败拖垮整题 | Semaphore(2) + 审计非致命 |
| `_verify_solution` | 简单题 `question_html` 13 字被整题 error | 答案完整则只 warning |

## 2. 全套 LLM 实测（服务器真 Key）

脚本 `_llm_short.py`，**PASS=18 FAIL=0**：

| 类别 | 项 | 结果 |
|---|---|---|
| 模型 | chat / solve / vision | PASS |
| Agent | h_dialogue / search / create_note | PASS |
| 备课 | prepare_lesson / import_and_prep | PASS |
| 补漏 | review_plan | PASS（列出余角补角等） |
| 联网 | web_search | PASS（4056 字，37s） |
| 图/算 | diagram / calc | PASS |
| 代码 | auto-render / errText / 侧栏 / add_q / process_sem / pdf_ingest | PASS |

## 3. 状态

- 本地 **340 passed**
- 线上 health **200**
- LLM 套件 **18/18**
- Git 本地提交待推（网络）
