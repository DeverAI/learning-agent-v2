# 2026-09-15 R40：桌面端 + 安卓端同步检修

## 1. 安卓端

**现状**：源码 v1.6 / versionCode 7（widget + SR 桥 + TTS 分片），但服务器 APK 还是 R24 的旧包。

**已重出 APK**：

- 构建：gradle 8.2 + JDK 17，`BUILD SUCCESSFUL`
- dex 符号核验：`LearningWidgetProvider / AndroidSR / SRBridge / pollEvents / AndroidTTS / splitForTts / resolveLaunchUrl` 全部 True
- 上传服务器：磁盘 2,984,456 字节
- 下载：`/dl/android` → 200，`application/vnd.android.package-archive`

**安卓 WebView 壳**：网页改动（MD、反馈页、智能上传、PDF 拆题）**自动生效**，无需重出 APK。

## 2. 桌面端

**现状**：PySide6 独立 UI，经 HTTP 调服务器 API：

| 模块 | 调用 | 兼容性 |
|---|---|---|
| lecture_engine | GET /api/papers, /api/questions | ✅ 未破坏 |
| classroom_sync | POST /api/notes | ✅ 未破坏 |
| settings | sync_server_url / sync_server_password | ✅ |

**已重出桌面端 zip**：`学习搭子-桌面端-20260915.zip`（1.3 MB）已上传服务器。

**未做**：PySide6 不在 miniforge 环境，桌面端离线回归 `test_r23_ask.py` 本轮未跑。

## 3. 我搞坏又修好的

PowerShell 对含中文文件名的 main.py / download.html 做替换 → **加了 BOM + 乱码** → 下载页 500。

**修**：从 `e0cab28` 恢复 + Python 手工替换文件名。

**教训**：以后**绝不用 PowerShell `-replace` 碰含中文的 .py/.html**，必须用 Python 或 Edit 工具。

## 4. 状态

- 本地 **345 passed**
- 线上 health **200**、下载页 **200**
- Git `43b4f8d`（push 待网络）
