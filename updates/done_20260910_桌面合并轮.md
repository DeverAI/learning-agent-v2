# 本轮已完成任务（2026-09-09/10，深度检修轮：桌面端合并 + 登记修复 + 服务器同步）

> 触发：用户要求"深度检修&优化前端设计&后端功能逻辑，字面意义上的检查——看代码和设计、想到问题/不对应/有歧义的点、验证、修复、记录；注意母文件夹下的文化课桌面端可以合并，整理文件夹结构"。
> 节奏：先合并+登记（用户"仅登记"指令）→ 用户解除限制"直接修复，修完同步服务器提供各端下载入口"→ 全量修复+验证+部署。
> 备份：`backups/merge_desktop_20260909/`（合并前文档 + 冗余备份 + _smoke_out.txt）；`backups/fix_round_20260909/`（16 个修复对象修复前副本 + M1/M3 探针脚本 + 部署脚本）；**服务器同步完成后整项目完整备份 `backups/full_project_20260910_064751/`（1196 文件 / 50.7MB，含 .git 与 storage，排除 backups 自身与缓存）**。

## 1. 桌面端合并（已完成）

- 来源 `all_projects/学习搭子桌面版`（OISystem 文化课分支）整仓拷入主仓 `desktop/`，源目录原样保留。
- 排除运行时数据与缓存（data/logs/backups/__pycache__/*.pyc）；两份冗余备份移入 backups；`Err.md`/`OISystem\Err.md` 重名拆分为 `Err.md`/`Err_OISystem.md`；补齐 `ui/themes/*.json`。
- 验证：源/目标文件集合比对一致；compileall 通过。
- 配套：.gitignore 增 desktop/data|logs|secrets；Design.md §3.2 登记桌面端组件行。

## 2. 修复清单（12 项，全部验证）

| # | 问题 | 修复 | 验证 |
|---|------|------|------|
| V1 | 5 个视觉调用点绕过统一入口直连 zhipuai_vision（ocr.py:770 拆题、diagram_service:547/1082/1130、layout_service:279） | 新增 `ai_service.vision_mimo_first()`（MiMo→ZhipuAI→None）；5 点全部替换；统一入口 `_call_vision_model_async` 删除 Kimi 视觉兜底（FreqErr 教训） | pytest 视觉链契约测试更新为"无 xm_key 跳 MiMo、链末 ZhipuAI 失败返回 None"通过；全仓对账仅剩 3 处合规调用 |
| M1 | focus 教学段 AI 失败吞成伪段落（has_checkpoint 逼学生对报错答"对吧"、虚增统计） | 失败段返回 `failed: True`；`_submit_checkpoint_locked` 对 failed 段按重试处理（pop 后重新生成，action=retry），不计 total/passed_checkpoints；focus.js 失败段用 `var(--err)` 斜体警示 | 服务层探针 5/5：retry 动作、计数不变、失败段被替换、正常段提交仍计检查点 |
| M2 | banks 四个写端点读改写竞态（SELECT→改→commit 跨 await） | 进程内全局 `_bank_write_lock`（asyncio.Lock）串行化 tags/add、tags/remove、rename、delete（题库低频，全局锁避免 rename 跨库锁序） | pytest + 代码核验 |
| M3 | worksheet regenerate mode=modify 丢题集（papers.py 传 question_ids 但 generate_worksheet 不读） | generate_worksheet 显式 question_ids 冻结题集：按 example_count 切分例题/练习（原卷保存顺序即 [例题...,练习...]），不重新检索；加 generation_warnings 提示 | 服务层探针 3/3：五题全保留、例题=前2、练习=后3、冻结模式不触检索 |
| M4 | 拆题失败静默按单题处理 | vision_mimo_first 判定失败自动重试 1 次；仍失败 `_mark_split_fallback` 写任务状态文件（split_fallback/split_fallback_reason），降级路径不再无痕 | 代码路径核验（常驻用户可见告警需 DB 字段，登记待决策） |
| M5 | Agent modify_note 绕过图谱同步 | content 字段 commit 后调用 `refresh_note_snippet`（notes.py 正规链同款容错） | 代码核验 |
| E1 | settings.json 带 UTF-8 BOM（9/8 22:34 外部工具写入）→ load_settings JSONDecodeError → 全部设置静默回退默认值 | `load_settings` 改 `encoding="utf-8-sig"`（对 BOM 透明，写侧仍无 BOM） | 修复前后 pytest 48→45 fail 的差额归因确认 |
| E2 | password_guard 逐请求读临时 settings 导致"未配置密码"时 expected="" 与测试 token 恒不等 → 45 个测试 401（上轮安全重构破坏了 import 快照时代测试的隐性契约） | 恢复既有语义：expected 为空=鉴权未启用=放行（未配置时本就拦不住空 header，非安全弱化）；配置密码仍恒定时间比较 | pytest 145/145 全绿 |
| F1 | agent.html 工具卡图标 '⚡'（违禁 emoji） | 改纯中文 '工' | 字符扫描 |
| F2 | questions.js 分页按钮 ←/→、diagnose.html 日志 '→' | 分页按钮去箭头；diagnose 改"警告：/提示：" | 字符扫描（剩余 4 处命中均为注释，不可见合规） |
| F3 | lecture.js 硬编码 #c00、lecture.html #888 | `var(--err)` / `var(--text3)`（无 hex 兜底，过主题契约扫描） | pytest test_theme_contract 通过 |
| F4 | lecture.js UTF-8 BOM | 去除 BOM（6786→6783 字节） | 字节检查 |
| T1 | test_vision_chain_skips_mimo_without_key 断言旧 Kimi 兜底契约 | 更新为新契约断言（result is None，calls==["zhipu"]），注明 FreqErr 依据 | pytest |
| T2 | test_tts_requires_auth 依赖旧快照语义 | 显式 monkeypatch `_get_auth_password` 启用鉴权再断言 401 | pytest |

## 3. 验证总账

- **pytest**：145/145（全量，含更新后的视觉链/鉴权/主题契约断言）+ R60 15/15 + R60b 12/12 + delivery 回归 0 fail；compileall 通过
- **服务层探针**：M1 focus 重试 5/5、M3 worksheet 冻结题集 3/3（探针脚本留档 backups/fix_round_20260909/）
- **字符/颜色扫描**：用户可见 emoji 0、分页箭头 0、业务色硬编码 0（剩余命中均为代码注释）
- **子 Agent 全局核验**：verifier 只读核验 **VERDICT: PASS**——A 桌面合并完整性 4/4（源目录未动、字节级一致、备份在位、gitignore/Design 登记）；B 修复证据 11 项全部属实（2 条轻微备注：_smoke_out.txt 在备份根而非子目录、拆题为共 2 次尝试——注释已按实际措辞修正）；C 全局完整性 4/4（12 文件内存编译通过、vision_mimo_first 1 定义+5 调用对账匹配、desktop 编译通过）
- **基线修复意义**：本轮修复前主仓 pytest 实际处于 48 fail 状态（BOM+鉴权语义双因），"160 基线"是上轮安全重构前记录的

## 4. 服务器同步与下载入口（2026-09-10）

- scp（paramiko 通道）推送 **20 个文件**（config/main + 3 routers + 5 services + 3 static/js + 6 模板含 download.html/base.html + 2 安装包）→ nssm restart → 服务 RUNNING，startup complete
- 下载入口页 `/download`（templates/download.html + base.html 侧边栏「下载」+ /download 路由）：APK v1.3 + 桌面端 zip（91 文件/0.5MB，排除运行时数据）+ PWA 说明；产物经 static/downloads 直出
- 服务器外部实测全部 200：health、/lecture、/download、APK（2975215B=本地一致）、桌面端 zip（497421B=本地一致）；部署脚本留档 backups/fix_round_20260909/deploy_learnagent.py，临时凭据已删除
- 注意：nssm restart 的 stop/start 在过渡窗口会返回 exit 1 + SERVICE_*_PENDING，health 需等 15s+ 再验；GET 才能验证页面（HEAD 405）

## 5. 登记未修（维持登记状态，待决策）

- 组卷 generate 同步挂起 20-30s（异步四件套待拍板）
- Agent save_image 死路（前端零引用，需产品决策：直传 ocr/upload）
- M4 常驻用户可见告警（需 DB 字段/任务面板支持）
- Design.md §5 措辞与 Fact.md MiMo-first 定规的调和（本轮按 Fact.md 落地代码，文档措辞待用户拍板）
- Design 12.7 与 5.1 权限边界措辞调和
- F2 遗留：/templates/_rollback 内历史文件含箭头（备份目录，不参与运行，未动）

## 6. FreqErr 新增

- [配置文件 BOM] 外部工具写出的 UTF-8 BOM JSON 让 load_settings 静默回退默认值 → 读侧 utf-8-sig
- [鉴权契约] password_guard 从 import 快照改逐请求读取时，破坏了"未配置密码=放行"的隐性契约（45 测试 401）→ 语义显式化：expected 空放行 + 测试显式启用鉴权
- [统一视觉入口旁路]（登记轮已录）本轮 5 个旁路点已清零

## 7. 看门狗 + 学校迷你板调研（2026-09-10 上午）

### 7.1 服务器看门狗（已上线）
- 需求：防 uvicorn 被系统杀掉后无人拉起 + 防"RUNNING 但 HTTP 假死"无人发现。
- 交付：`ops/watchdog_learningagent.py`（仓库留档）→ 服务器 `C:\all_projects\watchdog_learningagent.py`；nssm 服务 **LearningWatchdog**（RUNNING；脚本崩溃由 nssm 拉起，双层兜底）；`LearningAgent` 追加 `AppRestartDelay 5000`（被杀后 5s 再拉，避开端口释放窗口）。
- 策略：60s 探活 /api/health（3×10s）；非 RUNNING→start；RUNNING 假死→限流 restart（10min 冷却、连续 5 次熔断打 ALERT）；`watchdog.pause` 旗标暂停动作；netstat 端口占用只留痕不杀进程。
- 验证：本地 --dry-run 决策路径通过；服务器 --once 真探活 ok；服务上下文连续周期 ok（69s 间隔）；LearningWatchdog/LearningAgent 双 RUNNING。
- 运维：暂停=`type nul > C:\all_projects\watchdog.pause`，删除该文件恢复；日志 `C:\all_projects\watchdog_learningagent.log`（超 1MB 自动截断留尾）。

### 7.2 学校迷你板方案（调研稿，待拍板）
- 交付：`hardware/20260910-Mavis-学校迷你板方案.md`——选型对比、BOM 与价格、固件架构、服务器 `/api/board/snapshot` 设计、续航估算、P0-P3 路线、决策清单。
- 核心结论：主控选 **ESP32-S3**，不选 K210/K230——嘉楠（Canaan）已于 2025-06-23 整体关停 Kendryte AI 芯片业务（K210/K510/K230 全线停产、无支持路线图），且 K210 无 WiFi/BLE；显示选 **e-ink**（教室长亮唯一"无光害+月级续航"解）。首选整板 LilyGo T5 4.7" S3（约 ¥300）或自组 ESP32-S3+Waveshare 2.9"（约 ¥100）。
- 前提自检写入文档：纯看倒计时/课表可先用现成 PWA（零硬件成本）；真硬件价值在无手机干扰的桌面牌/班牌场景。
- 事实来源：jonpeddie.com（Canaan 退出 AI 的时间线）、lilygo.cc / espboards.dev（T5 规格与用户实测坑）、componentadvisor（e-ink 板对比）。

### 7.3 其他
- Err.log 清空（内容均为 09-09 前的 BOM 报错，修复已于上一轮上线验证）。
- 服务器凭据临时文件两次解密两次清零，未落明文残留。
