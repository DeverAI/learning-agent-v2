# 功能块清单（**从代码盘点，不是从文档**）

> 生成时间：2026-09-12 ｜ 方法：用 AST 解析 `backend/routers/*.py`、`models/models.py`、`main.py`、
> `desktop/**`、`android/**`，统计路由/导出/数据表，**不参考任何 .md**（避免被文档带偏）。
> 原始数据：`backups/_code_inventory.json` ｜ 生成脚本：`backups/_inventory_code.py`（可复跑）

## 代码规模事实

| 层 | 数字 |
|---|---|
| 后端路由文件 | **24** 个，共 **200** 条路由 |
| 后端 service | **32** 个文件，**17305** 行 |
| 数据表 | **10** 张（`models.py` 235 行） |
| 页面模板 | **22** 个（共 ~6600 行） |
| 前端 JS | **7** 个（~6650 行） |
| 桌面端 | **54** 个模块，**24508** 行 |
| Android | **2** 个 Java 文件 |
| `main.py` | 597 行（含 30 条页面路由 + 2 个中间件 + 1 个异常处理器） |

---

## 功能块清单（按数据流排序）

每条给出：**代码入口（文件:行数）→ 数据表 → 关键 service**。
「检修」列留给逐块走查时更新。

### 1. 题目主链路（上传 → OCR → 入库 → 编辑）
- 路由：`routers/ocr.py`（16 条，1398 行）、`routers/questions.py`（25 条，1435 行）
- 表：`questions`（38 列）、`processing_tasks`（9 列）、`upload_sessions`（10 列）
- service：`ocr_service.py`（1011）、`upload_guard.py`、`upload_session_service.py`
- 前端：`templates/batch_upload.html`、`questions.html`、`editor.html` + `static/js/questions.js`（1867）、`editor.js`（2217）
- 检修：**待做（第一批）**

### 2. 题图 / 示意图生成与渲染
- 路由：`routers/diagram.py`（16 条，408 行）、`routers/gallery.py`（8 条，130 行）
- service：`diagram_service.py`（1699）、`structure_graph_service.py`（872）、`calc_service.py`（603）、`layout_service.py`、`coord_engine.py`、`calibration_service.py`、`diagram_components/`
- 说明：这是**代码量最大的一块**（约 4000 行），含 SVG 生成、语义场景匹配、连线安全校验、标定
- 检修：待做

### 3. 题图搜索找题
- 路由：`routers/search.py`（4 条，630 行）
- 检修：**R23 读过后端全量（结论：三重校验/容量显式报错/原子抢占/恢复描述都到位）；
  断点在前端——题库侧原先对 `/api/notes` 零调用，R23 已补「存笔记」入口（桌面行+移动卡片+深链）**

### 4. 批改 / 纠错
- 路由：`routers/correction.py`（9 条，337 行）
- 表：`corrections`（15 列）
- service：`correction_service.py`（730）、`question_challenge.py`
- 前端：`correct.html`、`correct_center.html`、`correction_history.html`、`paper_correct.html`
- 检修：待做

### 5. 试卷与组卷
- 路由：`routers/papers.py`（12 条，680 行）、`routers/configs.py`（7 条，113 行）
- 表：`papers`（19 列）、`saved_configs`（6 列）
- service：`paper_service.py`（772）、`config_service.py`
- 前端：`papers.html`、`paper_generate.html`、`paper_detail.html`
- 检修：待做

### 6. 笔记与知识树
- 路由：`routers/notes.py`（15 条，1118 行）、`routers/knowledge_base.py`（5 条，43 行）
- 表：`notes`（17 列）
- service：`note_service.py`、`knowledge.py`、`tag_unification_service.py`
- 前端：`notes.html`（830 行）
- 检修：待做

### 7. 知识图谱 + 课程体系
- 路由：`routers/knowledge_graph.py`（13 条，174 行）
- service：`knowledge_graph.py`（940）、`curriculum_service.py`（约 380）
- 数据：`storage/knowledge_graph.json` + `data/curriculum_cn_junior.json`（206 知识点 / 211 前置边）
- 检修：**R18/R19 刚做完**（新增前置依赖与补漏清单），仍需走查

### 8. 讲题（Lecture）
- 路由：`routers/lecture.py`（5 条，61 行）
- service：`lecture_service.py`（499？实际 476）
- 前端：`lecture.html` + `static/js/lecture.js`（207）
- 桌面端：`desktop/core/lecture_engine.py`、`desktop/ui/lecture_view.py`
- 检修：R6–R9 检过一轮；**R23 复检 + 新增「边看边问」（提问带当前步骤上下文、不打断朗读）**

### 9. 专注模式 + 海马体（学习记忆）
- 路由：`routers/focus.py`（14 条，308 行，含 5 条 hippocampus 端点）
- service：`focus_service.py`（1054）、`hippocampus_service.py`、`face_analysis_service.py`
- 前端：`focus.html` + `static/js/focus.js`（996）
- 检修：R7 检过一轮（修了伪造值），待复检

### 10. Agent 对话与工具注册表
- 路由：`routers/sessions.py`（7 条，392 行）、`routers/chat.py`（3 条，618 行）
- service：`agent_core.py`、`agent_tools.py`（1693，**29 个工具**，R23 新增 `task_recall`）、`ai_service.py`（2141）
- 前端：`agent.html`（692）+ 页内 JS
- 检修：R11 重建过；**R23：后台任务完成 → 召回 Agent 继续讲解（工具+会话旁路+前端自动召回一次）**

### 11. 后台任务 / 双端推理
- 路由：`routers/agent_tasks.py`（4 条，46 行）
- 表：`agent_tasks`（14 列）
- service：`background_agent.py`、`session_locks.py`
- 检修：R14 新建，待复检

### 12. 备课与课稿
- 路由：`routers/lessons.py`（7 条，125 行）
- 表：`lessons`（12 列）
- service：`lesson_service.py`（499）
- 检修：R14 新建 + R17 修好生成；**R23：`/api/lessons` 进离线 GET 白名单（离线可看课稿）**

### 13. AI 服务层
- `services/ai_service.py`（2141 行，8 个顶层函数 + AIService 类大量方法）
- 覆盖：DeepSeek / Kimi / GLM / 小米 MiMo / 联网检索 / 视觉 / TTS
- 检修：R17 修过（reasoning 预算），待系统走查

### 14. 审计与自检
- service：`audit_service.py`（751）
- 路由：`routers/system.py`（6 条，224 行）、`routers/diagnose.py`（3 条，205 行）
- 前端：`diagnose.html`
- 检修：待做

### 15. 设置 / 配置 / 提示词
- 路由：`routers/settings.py`（4 条，511 行）、`routers/prompts.py`（7 条，131 行）、`routers/profile.py`（3 条，86 行）、`routers/banks.py`（6 条，138 行）
- 表：`prompt_templates`（8 列）
- 前端：`settings.html`（644）
- 检修：待做

### 16. 导出 / 下载
- service：`export_service.py`
- 路由：`papers.py` 的 `download` / `download-bundle`、`notes.py` 的 `{id}/download`
- 页面：`download.html`
- 检修：R12 修过下载页（改为读磁盘真实值）

### 17. 前端页面层（Web / PWA）
- `templates/base.html`(193) + 22 个页面 + `static/js/{app,questions,editor,focus,lecture,motion,offline}.js`
- PWA：`manifest.webmanifest`、`offline.js`
- 中间件：CSP 头、密码守卫（`main.py`）
- 检修：待做

### 18. 桌面端（PyQt）
- **54 个模块 / 24508 行**，含：
  - 课堂流与同步：`classroom_stream.py`、`classroom_sync.py`、`classroom_coach.py`
  - 讲题：`lecture_engine.py`、`ui/lecture_view.py`
  - 屏幕标注：`screen_paint.py`、`placement.py`、`screen_analyzer.py`
  - 语音：`audio_capture.py`、`vad.py`、`tts_player.py`
  - 监督与防作弊：`ai_supervisor.py`、`cheat_detector.py`、`site_guard.py`、`mute_mode.py`
  - 打断与 OJ：`interrupt_engine.py`、`oj_tracker.py`
  - UI：`sidebar*.py`、`graph_editor.py`、`md_renderer.py`、`themes.py`
- 检修：R8–R10 检过一轮，待复检

### 19. Android 端
- `MainActivity.java` + `TTSBridge`（WebView 壳 + 原生 TTS）
- 检修：R12 重出过 APK；**R23 全量读码：不是纯套壳**（拍照直拍/权限桥/下载同源限制/错误恢复都在），
  修 3 处：TTS 长文分片（4000 上限静默截断）、TTS 初始化竞态排队（丢首句）、讲课屏幕常亮；versionCode→6
- **待办**：WebView 无 Web Speech API → 安卓端语音输入不可用（需原生 SpeechRecognizer 桥或隐藏入口）

### 20. 基础设施
- `logger.py`（R16 修过标准流编码）、`config.py`、`models/database.py`、`models/models.py`、`upload_guard.py`、`session_locks.py`
- 安全：`main.py::password_guard`（R4 修过导出明文密码）

---

## 检修进度

| # | 功能块 | 状态 | 结论/产出 |
|---|---|---|---|
| 1 | 题目主链路 | **已检修（R19）** | 见 `updates/20260912_检修01_题目主链路.md`：链路整体扎实（三重上传校验/分块读/路径穿越防护/CAS 抢占/重启对账都在），**修了 3 处静默降级**；另记 8 项未修发现 |
| 2 | 题图生成与渲染 | **已检修（R22）** | 见 `updates/20260912_检修02_题图生成与渲染.md`：**修了三处**（消毒删 `style` 导致图上线条全部不可见 / 计算器单位与底数没告诉模型 / 分数吸附把无理数当精确分数并链式丢精度）；另记 12 项未修发现（E–Q） |
| 3 | 搜索找题 | 未开始 | — |
| 4 | 批改/纠错 | 未开始 | — |
| 5 | 试卷与组卷 | 未开始 | — |
| 6 | 笔记与知识树 | 未开始 | — |
| 7 | 知识图谱+课程体系 | 部分（R18/R19） | 见 `updates/20260912_课程体系与知识补漏闭环.md` |
| 8 | 讲题 | 部分（R6–R9） | 见 `updates/20260911_讲题链路审计与修复_R6R7.md` |
| 9 | 专注+海马体 | 部分（R7） | 见 `updates/20260911_讲题链路审计与修复_R6R7.md` |
| 10 | Agent 对话与工具 | 部分（R11） | 见 `updates/20260911_Agent工具注册表与内容接入_R11.md` |
| 11 | 后台任务 | 部分（R14） | 见 `updates/20260911_R12R14_下载入口与双端推理落地.md` |
| 12 | 备课与课稿 | 部分（R14/R17） | 同上 |
| 13 | AI 服务层 | 部分（R17） | 同上 |
| 14 | 审计与自检 | 部分（R19 改到 `audit_service` 的标记体系） | 见 `updates/20260912_检修01_题目主链路.md` |
| 15 | 设置/配置/提示词 | 未开始 | — |
| 16 | 导出/下载 | 部分（R12） | 同上 |
| 17 | 前端页面层 | 未开始 | — |
| 18 | 桌面端 | 部分（R8–R10） | 见 `updates/20260911_黑板子系统审计与8问判定_R10.md` |
| 19 | Android 端 | 未开始 | — |
| 20 | 基础设施 | 部分（R4/R16） | — |

**检修顺序原则**：按「用户实际会先用到哪条链路」排，而不是按代码量。
当前内容瓶颈在"把材料灌进来"，所以第 1 块（上传→OCR→入库）优先。

### 第 1 块遗留（下轮优先，已核实但未修）

A `/api/ocr/status/{task_id}` 死代码（前端 0 调用方）·
B 轮询网络失败静默停止 ·
C 轮询只看当前页 ·
D 同一张图重复上传产生两条题（无内容去重）·
E `_update_task` 找不到任务行静默 no-op ·
F 删除上传会话只删会话行、`staged` 题残留 ·
G 崩溃在"抢占"与"起任务"之间会卡 `pending` ·
H **待实测**：自动切题的子题题面图是否会画出别的小题

### 第 2 块遗留（下轮优先，已核实但未修）

E `_validate_svg_quality` **死代码**（70 行质检零调用；接线前要先修它未受保护的 `float(v)`）·
F 编辑器保存**硬编码 `index: 0`** → 编辑第 N 张图覆盖第 0 张 ·
G `regenerate` 路径 marker 与图**错位**（跳过/失败不补空占位）→ 可能渲染出下一条描述的图 ·
H 折线/坐标分支**落盘前零校验**，而消毒失败恰好返回空画布 → 空图当成功返回 ·
I 旧 `*.spec.json` **永不清理**，`fresh` 只看 `.ts` → 编辑器载入旧布局还显示"新鲜" ·
J 新鲜度检查**不校验 svg 是否存在**；DB 异常默认"有效" ·
K 组件**端口 `dx/dy` 不随缩放换算** → 拉大容器后下方组件摆错位置 ·
L 函数图圆按两轴缩放均值画正圆（应为椭圆）；非法 geo 元素静默丢弃 ·
M 坐标表达式求值失败**静默返回 0.0**；`tan(90°)` 极值支配自动缩放 ·
N 结构图校验阶段**静默丢节点与边**（重复 id/悬空边/环）且清洗结果直接落库 ·
O 结构图超限时错误措辞与真实原因不符 ·
P **待实测**：结构图文字宽度测量与折行两套模型不一致 ·
Q `calc_service` **零测试覆盖**、结构图仅 2 条测试 → L/M/N 的改动没有回归网

